---
description: Run the autobuild orchestrator for structured improvement cycles
allowed-tools: [Read, Write, Edit, Glob, Grep, Bash, Agent, AskUserQuestion, Skill]
argument-hint: "describe what you want to build or improve"
---

# Autobuild - Run Orchestrator

Invoke the `autobuild:run` skill and follow it exactly. It owns the whole procedure; this command only routes into it.

Pass the remainder of this invocation through unchanged as the skill's arguments.
