---
description: Export SVG to PNG with light/dark mode support via Playwright
allowed-tools: [Read, Bash, Glob, Grep, Skill]
argument-hint: "SVG file or glob, e.g. 'docs/images/01-diagram.svg' or 'docs/images/*.svg'"
---

# Export SVG to PNG

Invoke the `svg-infographics:export-png` skill and follow it exactly. It owns the whole procedure; this command only routes into it.

Pass the remainder of this invocation through unchanged as the skill's arguments.
