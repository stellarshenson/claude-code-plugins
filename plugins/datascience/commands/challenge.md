---
description: Apply the psychological prompting challenge - stakes, incentive, competitive framing. Use ONLY when the user explicitly asks for the challenge framing ("challenge me", "apply the challenge", "use competitive framing"); a hard problem alone is not the trigger, because the framing is a style the user opts into
allowed-tools: [Read, Write, Edit, Glob, Grep, Bash, Skill, Agent, AskUserQuestion]
argument-hint: "describe the problem that needs solving"
---

# Challenge Mode

Invoke the `datascience:challenge` skill and follow it exactly. It owns the whole procedure; this command only routes into it.

Pass the remainder of this invocation through unchanged as the skill's arguments.
