---
description: Create a new data science project from copier template
allowed-tools: [Read, Write, Edit, Bash, Glob, Grep, AskUserQuestion, Skill]
argument-hint: "project name and brief description"
---

# Create New Data Science Project

Invoke the `datascience:new-project` skill and follow it exactly. It owns the whole procedure; this command only routes into it.

Pass the remainder of this invocation through unchanged as the skill's arguments.
