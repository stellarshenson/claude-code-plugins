# Claude Code Journal

This journal tracks substantive work on documents, diagrams, and documentation content.

---

1. **Task - GitHub repo creation** (v0.1.0): Created GitHub repository stellarshenson/claude-code-plugins and pushed existing 3 commits (auto-build-claw plugin, docs, gitignore fixes)<br>
    **Result**: Repository live at https://github.com/stellarshenson/claude-code-plugins using git skill and vault skill for token retrieval from `~/.git-credentials`. Remote origin configured and tracking main branch.

2. **Task - Project scaffolding** (v0.1.0): Added Python project scaffolding from copier-data-science template, keeping only essentials for a plugins project - python module, tests, references, pyproject.toml, Makefile<br>
    **Result**: Generated scaffold via `copier copy` with uv/ruff/pytest tooling. Created `stellars_claude_code_plugins/` module with config/logging, `tests/` with pytest examples, `references/` directory. Makefile targets: install, test, lint, format, build. Stripped notebooks, data, reports, models, docker. Added MIT LICENSE, comprehensive .gitignore. Removed tracked .ipynb_checkpoints from history.

3. **Task - Architecture refactor plan** (v0.1.0): Evaluated whether central Python module + individual plugins architecture is viable, concluded it's the right structure but code didn't reflect it - the reusable 3020-line orchestration engine was buried inside auto-build-claw<br>
    **Result**: Designed extraction plan. Key insight: orchestrator is entirely YAML-driven so no subclassing needed - the whole engine belongs in the shared module, each plugin just provides YAML resources and a thin entrypoint. Deferred PyPI publishing until 2+ plugins exist.

4. **Task - Engine extraction and tests** (v0.1.0): Extracted orchestration engine from auto-build-claw into `stellars_claude_code_plugins/engine/` and wrote comprehensive test suite<br>
    **Result**: Moved `fsm.py` (258 lines), `model.py` (376 lines) to `engine/`. Created `engine/orchestrator.py` (2429 lines) with deferred initialization via `_initialize(resources_dir)` - model loading happens at `main()` call, not import time. Plugin's `orchestrate.py` reduced from 2386 lines to 18-line entrypoint that imports `main()` from engine and passes `resources_dir`. Removed dead `config.py` scaffolding, trimmed pyproject.toml deps to just pyyaml. Test suite: 115 tests total. All passing in 0.61s.

5. **Task - PROGRAM.md and BENCHMARK.md** (v0.8.44): Created program objective and benchmark checklist for auto-build-claw orchestrator execution targeting engine modernization<br>
    **Result**: `PROGRAM.md` defines workstreams: (A) replace custom FSM with `transitions` package, (B) remove HYPOTHESIS stage from PLANNING workflow, (C) tree-sitter code analysis. `BENCHMARK.md` contains checklist items with generative evaluation instructions. Score formula: `unchecked_items + failed_tests + (functions_over_complexity_10 * 2) + (unjustified_components * 3) + (unjustified_tests * 3)`.

6. **Task - Planning iteration 0** (v0.8.44): Auto-build-claw planning iteration for engine modernization program<br>
    **Result**: Research phase analyzed FSM migration scope (258 lines, 15 call sites, wrapper isolation means zero orchestrator changes), hypothesis removal scope (520 lines across 8 files), code complexity (17 functions >50 lines). Created 5-iteration implementation plan.

7. **Task - Program updates** (v0.8.45): Updated PROGRAM.md and BENCHMARK.md with additional workstreams D1 (independent workflow flag), D2 (run-until-complete), E (code justifications)<br>
    **Result**: PROGRAM.md expanded to 8 workstreams. Added `independent: false` field replacing `dependency: true`. Added CODE_JUSTIFICATIONS.md requirement. Benchmark score formula updated with unjustified_components and unjustified_tests at 3x weight.

8. **Task - Planning iteration 2** (v0.8.45): Auto-build-claw planning for remaining workstreams G (parallel gates), F (TEST benchmark enforcement), C (complexity refactoring)<br>
    **Result**: Research found cmd_end at 235 lines with 6 extraction points. Parallel gates feasible via ThreadPoolExecutor. Created 3-iteration plan.

9. **Task - Orchestrator complexity refactoring** (v0.8.45): Extract-method refactoring on three largest functions in `engine/orchestrator.py`<br>
    **Result**: Extracted 11 helper functions. `cmd_end` 235->57L (6 helpers), `_build_context` 109->59L (4 builders), `main` 115->35L (_build_cli_parser). All 115 tests pass unchanged.

10. **Task - Implementation iteration 1** (v0.8.45): Complexity refactoring, benchmark evaluation<br>
    **Result**: First proper BENCHMARK.md evaluation: score 17 (17 unchecked items, 0 test failures). Added Workstream H (benchmark agent) and Workstream G (parallel gates) to PROGRAM.md. Engine at 2348 lines orchestrator.py (down from 2444 baseline).

11. **Task - Implementation iteration 2** (v0.8.45): Benchmark enforcement in TEST phase<br>
    **Result**: Updated `_verify_test_phase` with MANDATORY benchmark instructions requiring file edit and Score Tracking update. Strengthened `phases.yaml` TEST end criteria and `agents.yaml` gatekeeper with 5-point benchmark rule. BENCHMARK.md evaluated: iter 2 score 21 (89/110 items passing). 21 unchecked items are unimplemented workstreams (parallel gates, benchmark agent) and intentional design decisions.

12. **Task - Benchmark score reduction** (v0.8.45): Four targeted fixes to reduce benchmark score from 15 toward 0<br>
    **Result**: (1) Removed `Agent.number` field entirely from model.py dataclass - orchestrator now derives agent number from `enumerate()` in `_build_agent_instructions`. Removed all `number: N` lines from agents.yaml (15 occurrences across RESEARCH, HYPOTHESIS, PLAN, IMPLEMENT, TEST, REVIEW, PLANNING phases). Updated agents.yaml header comments. Fixed 3 test assertions that referenced `agent.number`. (2) Replaced `State(str, Enum)` and `Event(str, Enum)` classes in fsm.py with plain string constants (`PENDING = "pending"`, `START = "start"`, etc.). FSM class methods now accept and return `str` instead of enum types. Updated `__init__.py` exports, orchestrator.py imports (removed `FSMState`/`FSMEvent` aliases, imported individual constants), and `_fire_fsm` signature. Rewrote all 115 test assertions in test_fsm.py from enum comparisons to string comparisons. (3) Added 3 benchmark-driven display messages to app.yaml (`benchmark_driven_label`, `benchmark_complete`, `benchmark_safety_cap`) and replaced 3 hardcoded strings in orchestrator.py `_run_next_iteration` and `_build_iter_label` with `_msg()` calls. Added messages to minimal test fixture in conftest.py. (4) Added `benchmark_evaluator` agent to `FULL::TEST` section in agents.yaml with prompt instructing to read BENCHMARK.md, evaluate items, edit file, update Score Tracking. All 115 tests pass, ruff format and ruff check clean.

13. **Task - Gate architecture refactor** (v0.8.45): Restructured agents.yaml gates from flat `readback`/`gatekeeper` keys to lifecycle-bound `on_start`/`on_end`/`on_skip` subsections, added `ActionDef` dataclass and `actions` section to workflow.yaml<br>
    **Result**: Refactored all 11 phase sections in `agents.yaml` from flat `gates: { readback: ..., gatekeeper: ... }` to nested `gates: { on_start: { readback: ... }, on_end: { gatekeeper: ... } }` structure - makes lifecycle binding explicit in YAML instead of hardcoded in orchestrator. Restructured `shared_gates` to wrap `gatekeeper_skip` and `gatekeeper_force_skip` under `on_skip` subsection. Updated `_build_agents_and_gates` in `model.py` to iterate through lifecycle subsections before extracting gate definitions - gate keys remain `PHASE::readback`/`PHASE::gatekeeper` for backward compatibility with orchestrator resolution. Added `ActionDef` dataclass (`type`, `description`, `prompt`) and `actions: dict[str, ActionDef]` field to `Model`. Created `_build_actions` builder function and added `_WORKFLOW_RESERVED_KEYS` set to prevent `actions` key from being parsed as a workflow type. Added 5 action definitions to `workflow.yaml` (3 programmatic: `plan_save`, `iteration_summary`, `iteration_advance`; 2 generative: `hypothesis_autowrite`, `hypothesis_gc` with prompts). Updated `_run_auto_actions` in `orchestrator.py` to fall through to generative action execution via `_claude_evaluate` when no Python handler exists. Updated `conftest.py` minimal fixture and `test_model.py` inline YAML to use new nested gate structure. Added 6 new tests: `test_gates_from_on_start_on_end`, `test_shared_gates_from_on_skip`, `test_actions_default_empty`, `test_real_actions_loaded`, `TestActionDef.test_programmatic_defaults`, `TestActionDef.test_generative_with_prompt`. All 121 tests pass, ruff clean, `orchestrate.py validate` reports 0 issues.

14. **Task - Remove hardcoded names** (v0.8.45): Eliminated all hardcoded gate and phase names from orchestrator.py, replacing them with model-driven property checks<br>
    **Result**: Six changes across `model.py`, `orchestrator.py`, and `phases.yaml`. (1) Added `start_gate_types`, `end_gate_types`, `skip_gate_types` sets to `Model` dataclass, populated during `_build_agents_and_gates` by tracking which gate types appear under `on_start`/`on_end`/`on_skip` YAML subsections. (2) Added `_resolve_lifecycle_gate(phase, lifecycle)` function that discovers gate type from model metadata instead of requiring a hardcoded name - replaced `_resolve_gate(phase, "readback")` with `_resolve_lifecycle_gate(phase, "start")` and `_resolve_gate(phase, "gatekeeper")` with `_resolve_lifecycle_gate(phase, "end")`. (3) Replaced hardcoded `"RESEARCH"`, `"HYPOTHESIS"`, `"PLAN"`, `"IMPLEMENT"`, `"TEST"`, `"REVIEW"` in `_run_summary` with iteration over `state["completed_phases"]`. (4) Replaced `phases[i] == "IMPLEMENT"` in `_prev_implementable` with model-driven logic: first checks current phase's `reject_to` declaration, then builds `reject_targets` set from all phases' `reject_to` and walks backward to find a target phase. (5) Replaced `if phase == "TEST"` in `cmd_end` with `phase_obj.auto_verify` property check. Added `auto_verify: bool = False` field to `Phase` dataclass and set `auto_verify: true` on `FULL::TEST` in phases.yaml. (6) Replaced `phase == "NEXT"` and `nxt == "NEXT"` in `_make_phase_callable` and `_action_iteration_summary` with `phase_obj.start_continue` property check - phases with conditional templates are detected by whether `start_continue` is defined. Also updated `_dry_run_phase` and `validate_model` to use `start_gate_types`/`end_gate_types` instead of hardcoded `("readback", "gatekeeper")`. Fixed `"PLAN"` string in `_run_next_iteration` to discover plan output from phases with `plan_save` auto_action. Added 7 new tests covering lifecycle gate resolution, prev_implementable fallback, gate metadata, and auto_verify. All 128 tests pass, ruff clean.

15. **Task - Benchmark items implementation** (v0.8.45): Added fast workflow, PLANNING::PLAN contrarian agent and ideation step, missing tests for generative action dispatch and action validation<br>
    **Result**: Five changes across 4 YAML resource files and 2 test files. (1) Added `fast` workflow to `workflow.yaml` with PLAN, IMPLEMENT, TEST, REVIEW, RECORD, NEXT(skippable) phases - skips RESEARCH and HYPOTHESIS for clear-objective iterations with no planning dependency. (2) Added `contrarian` agent to `PLANNING::PLAN` in `agents.yaml` with read-only prompt challenging plan alternatives, ordering, and assumptions. (3) Added ideation step to `PLANNING::PLAN` start template in `phases.yaml` instructing exploration of 2-3 alternative approaches with contrarian challenge before finalizing. (4) Added `TestGenerativeActionDispatch` in `test_orchestrator.py` with custom YAML fixtures containing a generative action wired to a phase, verifying `_claude_evaluate` is called with correct prompt and timeout. (5) Added `TestValidateModelActions` in `test_model.py` verifying `validate_model` catches undefined action references while not flagging valid ones. (6) Added `TestDryRunFastWorkflow` in `test_orchestrator.py` using real auto-build-claw resources, confirming dry-run produces PLAN through NEXT without RESEARCH/HYPOTHESIS. All 131 tests pass, ruff clean, model validates with 0 issues, fast workflow dry-run confirms correct phase resolution.

16. **Task - Agents into on_end gates** (v0.8.45): Moved agent definitions from phase-level into `gates.on_end` subsections across agents.yaml, model.py, and test fixtures<br>
    **Result**: Restructured all 7 phase sections with agents in `agents.yaml` (FULL::RESEARCH, FULL::HYPOTHESIS, FULL::PLAN, FULL::TEST, FULL::REVIEW, PLANNING::RESEARCH, PLANNING::PLAN) from phase-level `agents:` key to nested `gates: { on_end: { agents: [...], gatekeeper: {...} } }` structure - agents now live alongside the gatekeeper under `on_end` since they execute at phase end. Updated `_build_agents_and_gates` in `model.py` to look for agents inside `on_end` subsection first, falling back to phase-level `agents:` for backward compatibility. The `agents` key under `on_end` is skipped during gate extraction (not treated as a gate definition). Updated `conftest.py` minimal fixture and 3 inline YAML definitions in `test_model.py` and `test_orchestrator.py` to use the new structure. Added agent name validation to `validate_model`: checks that gate prompts referencing `{required_agents}` have agents defined for that phase, using the fallback resolution chain (WORKFLOW::PHASE -> bare PHASE -> FULL::PHASE) so GC::PLAN correctly resolves to FULL::PLAN's agents. Updated header comments in `agents.yaml` to document the new structure. All 131 tests pass, ruff format clean, `orchestrate.py validate` reports 0 issues.

17. **Task - FQN naming harmonisation** (v0.8.45): Harmonised all naming across YAML model into consistent FQN pattern NAMESPACE::NAME with cli_name for user-facing flags, merged agents.yaml into phases.yaml<br>
    **Result**: Five-part implementation. (1) `workflow.yaml`: renamed bare keys to `WORKFLOW::FULL`, `WORKFLOW::GC`, `WORKFLOW::HOTFIX`, `WORKFLOW::FAST`, `WORKFLOW::PLANNING` with `cli_name` attribute, `depends_on: WORKFLOW::PLANNING` uses FQN, actions renamed to `ACTION::PLAN_SAVE` etc with `cli_name`. (2) Merged all agent definitions and gate prompts from `agents.yaml` into `phases.yaml` under each phase's `gates:` subsection - deleted `agents.yaml`, reduced from 4 to 3 resource files. Shared phases (IMPLEMENT, TEST, REVIEW, PLAN, RECORD, NEXT) use bare names, workflow-specific (FULL::RESEARCH, FULL::HYPOTHESIS, GC::PLAN, PLANNING::RESEARCH, PLANNING::PLAN) retain FQN. (3) `model.py`: added `cli_name: str` to `WorkflowType` and `ActionDef`, `load_model` reads 3 files, replaced `_resolve_key` with strict `resolve_phase_key` that raises `KeyError` on missing (no FULL:: fallback), `validate_model` checks FQN format and cli_name uniqueness. (4) `orchestrator.py`: `ITERATION_TYPES` keyed by cli_name, `_CLI_TO_FQN` mapping, `_DEFAULT_CLI_NAME` from first independent workflow, `_workflow_prefix()` helper, removed all hardcoded `"full"` defaults. (5) Updated `conftest.py` (FQN fixtures, no agents.yaml), `test_model.py` (FQN assertions, strict resolution tests, cli_name tests), `test_orchestrator.py` (FQN workflow references). 141 tests pass, lint clean, validate 0 issues, all 4 dry-runs pass. Benchmark score 0 (39/39 items, harmony grade 10/10). Review: 4 agents all APPROVE.

18. **Task - Post-FQN GC cleanup** (v0.8.47): Removed dead code and stale references identified by review agents<br>
    **Result**: Three cleanup changes. (1) Replaced 4 stale `agents.yaml` references in `orchestrator.py` docstrings (lines 405, 958, 1003, 1867) with `phases.yaml`. (2) Removed orphan `agents.yaml` file creation in `tests/test_orchestrator.py` `TestGenerativeActionDispatch` fixture - merged gates/agents into the test's `phases.yaml` inline YAML, updated workflow to FQN format. (3) Removed dead `resolve_phase_key` function from `fsm.py` (lenient version superseded by strict version in `model.py`), updated `__init__.py` to export `resolve_phase_key` from `model.py` instead of `fsm.py`, removed 4 corresponding tests from `test_fsm.py`. 137 tests pass (4 fewer due to removed dead code tests), lint clean.

19. **Task - Bundled resources with auto-copy** (v0.8.47): YAML resources bundled in Python module with auto-copy to project-local directory on first use<br>
    **Result**: Copied 3 YAML resource files (workflow.yaml, phases.yaml, app.yaml) from `auto-build-claw/skills/auto-build-claw/resources/` into `stellars_claude_code_plugins/engine/resources/` as bundled defaults. Added `[tool.setuptools.package-data]` to `pyproject.toml` for wheel inclusion. Updated `main()` in `orchestrator.py` with resolution chain: `--resources-dir` CLI flag > project-local `.auto-build-claw/resources/` (auto-copied from module if missing) > bundled module resources. Added `_ensure_project_resources()` that creates project dir and copies only missing files (preserves user customizations). Fixed critical F1 defect found by forensicist: `_clean_artifacts_dir` was destroying `resources/` subdirectory - added `_CLEAN_PRESERVE_DIRS = {"resources"}` to preserve it. Removed old skill resources directory and redundant `orchestrate.py` entrypoint (pip-installed `orchestrate` CLI is the only entry point now). Moved `shutil` import to top-level. 5 new tests: `test_bundled_resources_exist`, `test_copies_missing_resources`, `test_does_not_overwrite_existing`, `test_loads_from_project_resources`, `test_clean_preserves_resources`. 142 tests pass, lint clean. `orchestrate validate` and `orchestrate new --dry-run` both work without `--resources-dir`.

20. **Task - Medium article update** (v0.8.47): Rewrote pull-based workflow enforcement article to match current architecture, added theoretical foundations with academic citations<br>
    **Result**: Complete rewrite of `docs/medium/article_01_pull-based-workflow-enforcement.md` from 2384 to 1639 words. Fixed 12 inaccuracy categories: wrong repo URL (4 places), agents.yaml references, orchestrate.py vs CLI, bare workflow names, old 4-file structure, wrong fallback chain, missing FQN. Added program-driven execution section explaining PROGRAM.md + BENCHMARK.md as objective function (inspired by Karpathy's autoresearch). Added theoretical foundations section grounded in 5 academic papers: context coherence (Liu et al. 2023 "Lost in the Middle"), phase-boundary reinforcement (Han et al. 2024 "LLM Multi-Agent Systems"), readback as course correction (Deng et al. 2023 "Rephrase and Respond"), independent evaluation (Ye et al. 2024 "Justice or Prejudice?"), objective convergence (Madaan et al. 2023 "Self-Refine"). Downloaded all 5 papers to `references/`. Created 3 new SVGs: `09-multi-agent-defect-detection.svg` (4 agents catching different defect classes), `10-end-to-end-journey.svg` (program-driven execution flow), `11-theoretical-foundations.svg` (5 theoretical concepts with paper citations). Updated SVGs `04-content-engine-separation` (3 files, FQN) and `05-full-workflow-agents` (TEST=1 agent, total 15). Added real transcript of forensicist catching F1 defect. "Separate process" reduced from 13 mentions to 1. Both session transcripts preserved. Benchmark score: 4.0 -> fixes applied for strict-lookup and wording.

21. **Task - Article 01b editorial tightening** (v0.8.47): Tightened article from 7.5/10 to ~8.5/10 based on editorial critique and devil's advocate analysis<br>
    **Result**: Created `article_01b_pull-based-workflow-enforcement.md` as working copy. Applied devil's advocate methodology with Fibonacci-scored concern catalogue (7 concerns, total risk 194, baseline residual 135). Rewrote opening from declarative ("AI coding agents generate impressive code") to transcript-led hook showing agent rationalising a skip attempt. Eliminated "external enforcement" synonym cluster from 7 variants to 1 body instance. Compressed theoretical foundations from 5 bold paragraphs to 1 dense paragraph with inline citations. Removed 3 decorative SVGs (06-five-failure-modes, 07-three-principles, 11-theoretical-foundations) reducing from 11 to 8 images. Rewrote closing from polite "the method matters more than the tool" to doctrine: "Agents do not need better prompts. They need constraints they cannot override." New title: "Your AI Agent Will Cut Corners. Here's How to Stop It." Iteration 14 fixed remaining synonym clusters (enforcement 4x->1x, process-isolation consolidated to Principle 2), removed redundant gate-enforcement subsection, fixed SVG adjacency. Word count: 1352 (from 1639). Benchmark score: 18->15 (editorial 8, arc 8, voice 8, opening 8, middle 8, closing 8, specificity 9).

22. **Task - Start/execution/end lifecycle restructure**: Rewrote `model.py` to support new start/execution/end YAML structure while maintaining backward compatibility with legacy gates.on_start/on_end format<br>
    **Result**: Five changes to `stellars_claude_code_plugins/engine/model.py` (656->820 lines). (1) `_build_phases` rewritten from one-liner dict comprehension to explicit builder that extracts templates from `start.template` and `end.template` when `start`/`end` are dicts, falls back to flat string when they're strings (legacy), and reads `template_continue`/`template_final` variants from inside the dict. Added `_PHASE_LIFECYCLE_KEYS = {"start", "execution", "end"}` to skip these during non-lifecycle field extraction. (2) `_build_agents_and_gates` rewritten with dual-path logic: new path reads `start.agents` as Gate objects (keyed `PHASE::agent_name`), `execution.agents` as Agent objects, `end.agents` as Gate objects; legacy path activates only when `found_new_structure` is False and `gates` key exists. `shared_gates` now accepts both `skip` (new) and `on_skip` (legacy) subsection keys. Removed `_LIFECYCLE_MAP` from module level (now local to legacy branch only). Return type expanded to 6-tuple adding `legacy_gate_phases` set. (3) `Model` dataclass gained `legacy_gate_phases: set[str]` field tracking phases that used old `gates:` structure. (4) `validate_model` adds deprecation warning for each phase in `legacy_gate_phases`, updated error messages to reference "start/end section" instead of "gates.on_start/on_end". (5) `load_model` unpacks 6-tuple from `_build_agents_and_gates`. All 143 tests pass (57 model + 86 orchestrator), ruff clean.
