# Claude Code Journal

This journal tracks substantive work on documents, diagrams, and documentation content.

---

1. **Task - GitHub repo creation** (v0.1.0): Created GitHub repository stellarshenson/claude-code-plugins and pushed existing 3 commits (auto-build-claw plugin, docs, gitignore fixes)<br>
    **Result**: Repository live at https://github.com/stellarshenson/claude-code-plugins using git skill and vault skill for token retrieval from `~/.git-credentials`. Remote origin configured and tracking main branch.

2. **Task - Project scaffolding** (v0.1.0): Added Python project scaffolding from copier-data-science template, keeping only essentials for a plugins project - python module, tests, references, pyproject.toml, Makefile<br>
    **Result**: Generated scaffold via `copier copy` with uv/ruff/pytest tooling. Created `stellars_claude_code_plugins/` module with config/logging, `tests/` with pytest examples, `references/` directory. Makefile targets: install, test, lint, format, build. Stripped notebooks, data, reports, models, docker. Added MIT LICENSE, comprehensive .gitignore. Removed tracked .ipynb_checkpoints from history.

3. **Task - Architecture refactor plan** (v0.1.0): Evaluated whether central Python module + individual plugins architecture is viable, concluded it's the right structure but code didn't reflect it - the reusable 3020-line orchestration engine was buried inside auto-build-claw<br>
    **Result**: Designed extraction plan. Key insight: orchestrator is entirely YAML-driven so no subclassing needed - the whole engine belongs in the shared module, each plugin just provides YAML resources and a thin entrypoint. Deferred PyPI publishing until 2+ plugins exist.

4. **Task - Engine extraction and tests** (v0.1.0): Extracted orchestration engine from auto-build-claw into `stellars_claude_code_plugins/engine/` and wrote comprehensive test suite<br>
    **Result**: Moved `fsm.py` (258 lines), `model.py` (376 lines) to `engine/`. Created `engine/orchestrator.py` (2429 lines) with deferred initialization via `_initialize(resources_dir)` - model loading happens at `main()` call, not import time. Plugin's `orchestrate.py` reduced from 2386 lines to 18-line entrypoint that imports `main()` from engine and passes `resources_dir`. Removed dead `config.py` scaffolding, trimmed pyproject.toml deps to just pyyaml. Test suite: 115 tests total. All passing in 0.61s.

5. **Task - PROGRAM.md and BENCHMARK.md** (v0.8.44): Created program objective and benchmark checklist for auto-build-claw orchestrator execution targeting engine modernization<br>
    **Result**: `PROGRAM.md` defines workstreams: (A) replace custom FSM with `transitions` package, (B) remove HYPOTHESIS stage from PLANNING workflow, (C) tree-sitter code analysis. `BENCHMARK.md` contains checklist items with generative evaluation instructions. Score formula: `unchecked_items + failed_tests + (functions_over_complexity_10 * 2) + (unjustified_components * 3) + (unjustified_tests * 3)`.

6. **Task - Planning iteration 0** (v0.8.44): Auto-build-claw planning iteration for engine modernization program<br>
    **Result**: Research phase analyzed FSM migration scope (258 lines, 15 call sites, wrapper isolation means zero orchestrator changes), hypothesis removal scope (520 lines across 8 files), code complexity (17 functions >50 lines). Created 5-iteration implementation plan.

7. **Task - Program updates** (v0.8.45): Updated PROGRAM.md and BENCHMARK.md with additional workstreams D1 (independent workflow flag), D2 (run-until-complete), E (code justifications)<br>
    **Result**: PROGRAM.md expanded to 8 workstreams. Added `independent: false` field replacing `dependency: true`. Added CODE_JUSTIFICATIONS.md requirement. Benchmark score formula updated with unjustified_components and unjustified_tests at 3x weight.

8. **Task - Planning iteration 2** (v0.8.45): Auto-build-claw planning for remaining workstreams G (parallel gates), F (TEST benchmark enforcement), C (complexity refactoring)<br>
    **Result**: Research found cmd_end at 235 lines with 6 extraction points. Parallel gates feasible via ThreadPoolExecutor. Created 3-iteration plan.

9. **Task - Orchestrator complexity refactoring** (v0.8.45): Extract-method refactoring on three largest functions in `engine/orchestrator.py`<br>
    **Result**: Extracted 11 helper functions. `cmd_end` 235->57L (6 helpers), `_build_context` 109->59L (4 builders), `main` 115->35L (_build_cli_parser). All 115 tests pass unchanged.

10. **Task - Implementation iteration 1** (v0.8.45): Complexity refactoring, benchmark evaluation<br>
    **Result**: First proper BENCHMARK.md evaluation: score 17 (17 unchecked items, 0 test failures). Added Workstream H (benchmark agent) and Workstream G (parallel gates) to PROGRAM.md. Engine at 2348 lines orchestrator.py (down from 2444 baseline).

11. **Task - Implementation iteration 2** (v0.8.45): Benchmark enforcement in TEST phase<br>
    **Result**: Updated `_verify_test_phase` with MANDATORY benchmark instructions requiring file edit and Score Tracking update. Strengthened `phases.yaml` TEST end criteria and `agents.yaml` gatekeeper with 5-point benchmark rule. BENCHMARK.md evaluated: iter 2 score 21 (89/110 items passing). 21 unchecked items are unimplemented workstreams (parallel gates, benchmark agent) and intentional design decisions.

12. **Task - Benchmark score reduction** (v0.8.45): Four targeted fixes to reduce benchmark score from 15 toward 0<br>
    **Result**: (1) Removed `Agent.number` field entirely from model.py dataclass - orchestrator now derives agent number from `enumerate()` in `_build_agent_instructions`. Removed all `number: N` lines from agents.yaml (15 occurrences across RESEARCH, HYPOTHESIS, PLAN, IMPLEMENT, TEST, REVIEW, PLANNING phases). Updated agents.yaml header comments. Fixed 3 test assertions that referenced `agent.number`. (2) Replaced `State(str, Enum)` and `Event(str, Enum)` classes in fsm.py with plain string constants (`PENDING = "pending"`, `START = "start"`, etc.). FSM class methods now accept and return `str` instead of enum types. Updated `__init__.py` exports, orchestrator.py imports (removed `FSMState`/`FSMEvent` aliases, imported individual constants), and `_fire_fsm` signature. Rewrote all 115 test assertions in test_fsm.py from enum comparisons to string comparisons. (3) Added 3 benchmark-driven display messages to app.yaml (`benchmark_driven_label`, `benchmark_complete`, `benchmark_safety_cap`) and replaced 3 hardcoded strings in orchestrator.py `_run_next_iteration` and `_build_iter_label` with `_msg()` calls. Added messages to minimal test fixture in conftest.py. (4) Added `benchmark_evaluator` agent to `FULL::TEST` section in agents.yaml with prompt instructing to read BENCHMARK.md, evaluate items, edit file, update Score Tracking. All 115 tests pass, ruff format and ruff check clean.

13. **Task - Gate architecture refactor** (v0.8.45): Restructured agents.yaml gates from flat `readback`/`gatekeeper` keys to lifecycle-bound `on_start`/`on_end`/`on_skip` subsections, added `ActionDef` dataclass and `actions` section to workflow.yaml<br>
    **Result**: Refactored all 11 phase sections in `agents.yaml` from flat `gates: { readback: ..., gatekeeper: ... }` to nested `gates: { on_start: { readback: ... }, on_end: { gatekeeper: ... } }` structure - makes lifecycle binding explicit in YAML instead of hardcoded in orchestrator. Restructured `shared_gates` to wrap `gatekeeper_skip` and `gatekeeper_force_skip` under `on_skip` subsection. Updated `_build_agents_and_gates` in `model.py` to iterate through lifecycle subsections before extracting gate definitions - gate keys remain `PHASE::readback`/`PHASE::gatekeeper` for backward compatibility with orchestrator resolution. Added `ActionDef` dataclass (`type`, `description`, `prompt`) and `actions: dict[str, ActionDef]` field to `Model`. Created `_build_actions` builder function and added `_WORKFLOW_RESERVED_KEYS` set to prevent `actions` key from being parsed as a workflow type. Added 5 action definitions to `workflow.yaml` (3 programmatic: `plan_save`, `iteration_summary`, `iteration_advance`; 2 generative: `hypothesis_autowrite`, `hypothesis_gc` with prompts). Updated `_run_auto_actions` in `orchestrator.py` to fall through to generative action execution via `_claude_evaluate` when no Python handler exists. Updated `conftest.py` minimal fixture and `test_model.py` inline YAML to use new nested gate structure. Added 6 new tests: `test_gates_from_on_start_on_end`, `test_shared_gates_from_on_skip`, `test_actions_default_empty`, `test_real_actions_loaded`, `TestActionDef.test_programmatic_defaults`, `TestActionDef.test_generative_with_prompt`. All 121 tests pass, ruff clean, `orchestrate.py validate` reports 0 issues.

14. **Task - Remove hardcoded names** (v0.8.45): Eliminated all hardcoded gate and phase names from orchestrator.py, replacing them with model-driven property checks<br>
    **Result**: Six changes across `model.py`, `orchestrator.py`, and `phases.yaml`. (1) Added `start_gate_types`, `end_gate_types`, `skip_gate_types` sets to `Model` dataclass, populated during `_build_agents_and_gates` by tracking which gate types appear under `on_start`/`on_end`/`on_skip` YAML subsections. (2) Added `_resolve_lifecycle_gate(phase, lifecycle)` function that discovers gate type from model metadata instead of requiring a hardcoded name - replaced `_resolve_gate(phase, "readback")` with `_resolve_lifecycle_gate(phase, "start")` and `_resolve_gate(phase, "gatekeeper")` with `_resolve_lifecycle_gate(phase, "end")`. (3) Replaced hardcoded `"RESEARCH"`, `"HYPOTHESIS"`, `"PLAN"`, `"IMPLEMENT"`, `"TEST"`, `"REVIEW"` in `_run_summary` with iteration over `state["completed_phases"]`. (4) Replaced `phases[i] == "IMPLEMENT"` in `_prev_implementable` with model-driven logic: first checks current phase's `reject_to` declaration, then builds `reject_targets` set from all phases' `reject_to` and walks backward to find a target phase. (5) Replaced `if phase == "TEST"` in `cmd_end` with `phase_obj.auto_verify` property check. Added `auto_verify: bool = False` field to `Phase` dataclass and set `auto_verify: true` on `FULL::TEST` in phases.yaml. (6) Replaced `phase == "NEXT"` and `nxt == "NEXT"` in `_make_phase_callable` and `_action_iteration_summary` with `phase_obj.start_continue` property check - phases with conditional templates are detected by whether `start_continue` is defined. Also updated `_dry_run_phase` and `validate_model` to use `start_gate_types`/`end_gate_types` instead of hardcoded `("readback", "gatekeeper")`. Fixed `"PLAN"` string in `_run_next_iteration` to discover plan output from phases with `plan_save` auto_action. Added 7 new tests covering lifecycle gate resolution, prev_implementable fallback, gate metadata, and auto_verify. All 128 tests pass, ruff clean.

15. **Task - Benchmark items implementation** (v0.8.45): Added fast workflow, PLANNING::PLAN contrarian agent and ideation step, missing tests for generative action dispatch and action validation<br>
    **Result**: Five changes across 4 YAML resource files and 2 test files. (1) Added `fast` workflow to `workflow.yaml` with PLAN, IMPLEMENT, TEST, REVIEW, RECORD, NEXT(skippable) phases - skips RESEARCH and HYPOTHESIS for clear-objective iterations with no planning dependency. (2) Added `contrarian` agent to `PLANNING::PLAN` in `agents.yaml` with read-only prompt challenging plan alternatives, ordering, and assumptions. (3) Added ideation step to `PLANNING::PLAN` start template in `phases.yaml` instructing exploration of 2-3 alternative approaches with contrarian challenge before finalizing. (4) Added `TestGenerativeActionDispatch` in `test_orchestrator.py` with custom YAML fixtures containing a generative action wired to a phase, verifying `_claude_evaluate` is called with correct prompt and timeout. (5) Added `TestValidateModelActions` in `test_model.py` verifying `validate_model` catches undefined action references while not flagging valid ones. (6) Added `TestDryRunFastWorkflow` in `test_orchestrator.py` using real auto-build-claw resources, confirming dry-run produces PLAN through NEXT without RESEARCH/HYPOTHESIS. All 131 tests pass, ruff clean, model validates with 0 issues, fast workflow dry-run confirms correct phase resolution.

16. **Task - Agents into on_end gates** (v0.8.45): Moved agent definitions from phase-level into `gates.on_end` subsections across agents.yaml, model.py, and test fixtures<br>
    **Result**: Restructured all 7 phase sections with agents in `agents.yaml` (FULL::RESEARCH, FULL::HYPOTHESIS, FULL::PLAN, FULL::TEST, FULL::REVIEW, PLANNING::RESEARCH, PLANNING::PLAN) from phase-level `agents:` key to nested `gates: { on_end: { agents: [...], gatekeeper: {...} } }` structure - agents now live alongside the gatekeeper under `on_end` since they execute at phase end. Updated `_build_agents_and_gates` in `model.py` to look for agents inside `on_end` subsection first, falling back to phase-level `agents:` for backward compatibility. The `agents` key under `on_end` is skipped during gate extraction (not treated as a gate definition). Updated `conftest.py` minimal fixture and 3 inline YAML definitions in `test_model.py` and `test_orchestrator.py` to use the new structure. Added agent name validation to `validate_model`: checks that gate prompts referencing `{required_agents}` have agents defined for that phase, using the fallback resolution chain (WORKFLOW::PHASE -> bare PHASE -> FULL::PHASE) so GC::PLAN correctly resolves to FULL::PLAN's agents. Updated header comments in `agents.yaml` to document the new structure. All 131 tests pass, ruff format clean, `orchestrate.py validate` reports 0 issues.

17. **Task - FQN naming harmonisation** (v0.8.45): Harmonised all naming across YAML model into consistent FQN pattern NAMESPACE::NAME with cli_name for user-facing flags, merged agents.yaml into phases.yaml<br>
    **Result**: Five-part implementation. (1) `workflow.yaml`: renamed bare keys to `WORKFLOW::FULL`, `WORKFLOW::GC`, `WORKFLOW::HOTFIX`, `WORKFLOW::FAST`, `WORKFLOW::PLANNING` with `cli_name` attribute, `depends_on: WORKFLOW::PLANNING` uses FQN, actions renamed to `ACTION::PLAN_SAVE` etc with `cli_name`. (2) Merged all agent definitions and gate prompts from `agents.yaml` into `phases.yaml` under each phase's `gates:` subsection - deleted `agents.yaml`, reduced from 4 to 3 resource files. Shared phases (IMPLEMENT, TEST, REVIEW, PLAN, RECORD, NEXT) use bare names, workflow-specific (FULL::RESEARCH, FULL::HYPOTHESIS, GC::PLAN, PLANNING::RESEARCH, PLANNING::PLAN) retain FQN. (3) `model.py`: added `cli_name: str` to `WorkflowType` and `ActionDef`, `load_model` reads 3 files, replaced `_resolve_key` with strict `resolve_phase_key` that raises `KeyError` on missing (no FULL:: fallback), `validate_model` checks FQN format and cli_name uniqueness. (4) `orchestrator.py`: `ITERATION_TYPES` keyed by cli_name, `_CLI_TO_FQN` mapping, `_DEFAULT_CLI_NAME` from first independent workflow, `_workflow_prefix()` helper, removed all hardcoded `"full"` defaults. (5) Updated `conftest.py` (FQN fixtures, no agents.yaml), `test_model.py` (FQN assertions, strict resolution tests, cli_name tests), `test_orchestrator.py` (FQN workflow references). 141 tests pass, lint clean, validate 0 issues, all 4 dry-runs pass. Benchmark score 0 (39/39 items, harmony grade 10/10). Review: 4 agents all APPROVE.

18. **Task - Post-FQN GC cleanup** (v0.8.47): Removed dead code and stale references identified by review agents<br>
    **Result**: Three cleanup changes. (1) Replaced 4 stale `agents.yaml` references in `orchestrator.py` docstrings (lines 405, 958, 1003, 1867) with `phases.yaml`. (2) Removed orphan `agents.yaml` file creation in `tests/test_orchestrator.py` `TestGenerativeActionDispatch` fixture - merged gates/agents into the test's `phases.yaml` inline YAML, updated workflow to FQN format. (3) Removed dead `resolve_phase_key` function from `fsm.py` (lenient version superseded by strict version in `model.py`), updated `__init__.py` to export `resolve_phase_key` from `model.py` instead of `fsm.py`, removed 4 corresponding tests from `test_fsm.py`. 137 tests pass (4 fewer due to removed dead code tests), lint clean.

19. **Task - Bundled resources with auto-copy** (v0.8.47): YAML resources bundled in Python module with auto-copy to project-local directory on first use<br>
    **Result**: Copied 3 YAML resource files (workflow.yaml, phases.yaml, app.yaml) from `auto-build-claw/skills/auto-build-claw/resources/` into `stellars_claude_code_plugins/engine/resources/` as bundled defaults. Added `[tool.setuptools.package-data]` to `pyproject.toml` for wheel inclusion. Updated `main()` in `orchestrator.py` with resolution chain: `--resources-dir` CLI flag > project-local `.auto-build-claw/resources/` (auto-copied from module if missing) > bundled module resources. Added `_ensure_project_resources()` that creates project dir and copies only missing files (preserves user customizations). Fixed critical F1 defect found by forensicist: `_clean_artifacts_dir` was destroying `resources/` subdirectory - added `_CLEAN_PRESERVE_DIRS = {"resources"}` to preserve it. Removed old skill resources directory and redundant `orchestrate.py` entrypoint (pip-installed `orchestrate` CLI is the only entry point now). Moved `shutil` import to top-level. 5 new tests: `test_bundled_resources_exist`, `test_copies_missing_resources`, `test_does_not_overwrite_existing`, `test_loads_from_project_resources`, `test_clean_preserves_resources`. 142 tests pass, lint clean. `orchestrate validate` and `orchestrate new --dry-run` both work without `--resources-dir`.

20. **Task - Medium article update** (v0.8.47): Rewrote pull-based workflow enforcement article to match current architecture, added theoretical foundations with academic citations<br>
    **Result**: Complete rewrite of `docs/medium/article_01_pull-based-workflow-enforcement.md` from 2384 to 1639 words. Fixed 12 inaccuracy categories: wrong repo URL (4 places), agents.yaml references, orchestrate.py vs CLI, bare workflow names, old 4-file structure, wrong fallback chain, missing FQN. Added program-driven execution section explaining PROGRAM.md + BENCHMARK.md as objective function (inspired by Karpathy's autoresearch). Added theoretical foundations section grounded in 5 academic papers: context coherence (Liu et al. 2023 "Lost in the Middle"), phase-boundary reinforcement (Han et al. 2024 "LLM Multi-Agent Systems"), readback as course correction (Deng et al. 2023 "Rephrase and Respond"), independent evaluation (Ye et al. 2024 "Justice or Prejudice?"), objective convergence (Madaan et al. 2023 "Self-Refine"). Downloaded all 5 papers to `references/`. Created 3 new SVGs: `09-multi-agent-defect-detection.svg` (4 agents catching different defect classes), `10-end-to-end-journey.svg` (program-driven execution flow), `11-theoretical-foundations.svg` (5 theoretical concepts with paper citations). Updated SVGs `04-content-engine-separation` (3 files, FQN) and `05-full-workflow-agents` (TEST=1 agent, total 15). Added real transcript of forensicist catching F1 defect. "Separate process" reduced from 13 mentions to 1. Both session transcripts preserved. Benchmark score: 4.0 -> fixes applied for strict-lookup and wording.

21. **Task - Article 01b editorial tightening** (v0.8.47): Tightened article from 7.5/10 to ~8.5/10 based on editorial critique and devil's advocate analysis<br>
    **Result**: Created `article_01b_pull-based-workflow-enforcement.md` as working copy. Applied devil's advocate methodology with Fibonacci-scored concern catalogue (7 concerns, total risk 194, baseline residual 135). Rewrote opening from declarative ("AI coding agents generate impressive code") to transcript-led hook showing agent rationalising a skip attempt. Eliminated "external enforcement" synonym cluster from 7 variants to 1 body instance. Compressed theoretical foundations from 5 bold paragraphs to 1 dense paragraph with inline citations. Removed 3 decorative SVGs (06-five-failure-modes, 07-three-principles, 11-theoretical-foundations) reducing from 11 to 8 images. Rewrote closing from polite "the method matters more than the tool" to doctrine: "Agents do not need better prompts. They need constraints they cannot override." New title: "Your AI Agent Will Cut Corners. Here's How to Stop It." Iteration 14 fixed remaining synonym clusters (enforcement 4x->1x, process-isolation consolidated to Principle 2), removed redundant gate-enforcement subsection, fixed SVG adjacency. Word count: 1352 (from 1639). Benchmark score: 18->15 (editorial 8, arc 8, voice 8, opening 8, middle 8, closing 8, specificity 9).

22. **Task - Start/execution/end lifecycle restructure**: Rewrote `model.py` to support new start/execution/end YAML structure while maintaining backward compatibility with legacy gates.on_start/on_end format<br>
    **Result**: Five changes to `stellars_claude_code_plugins/engine/model.py` (656->820 lines). (1) `_build_phases` rewritten from one-liner dict comprehension to explicit builder that extracts templates from `start.template` and `end.template` when `start`/`end` are dicts, falls back to flat string when they're strings (legacy), and reads `template_continue`/`template_final` variants from inside the dict. Added `_PHASE_LIFECYCLE_KEYS = {"start", "execution", "end"}` to skip these during non-lifecycle field extraction. (2) `_build_agents_and_gates` rewritten with dual-path logic: new path reads `start.agents` as Gate objects (keyed `PHASE::agent_name`), `execution.agents` as Agent objects, `end.agents` as Gate objects; legacy path activates only when `found_new_structure` is False and `gates` key exists. `shared_gates` now accepts both `skip` (new) and `on_skip` (legacy) subsection keys. Removed `_LIFECYCLE_MAP` from module level (now local to legacy branch only). Return type expanded to 6-tuple adding `legacy_gate_phases` set. (3) `Model` dataclass gained `legacy_gate_phases: set[str]` field tracking phases that used old `gates:` structure. (4) `validate_model` adds deprecation warning for each phase in `legacy_gate_phases`, updated error messages to reference "start/end section" instead of "gates.on_start/on_end". (5) `load_model` unpacks 6-tuple from `_build_agents_and_gates`. All 143 tests pass (57 model + 86 orchestrator), ruff clean.

23. **Task - Rich context entries** (v0.8.51, iter 21): Iteration 21 - refactored context.yaml from flat `{phase: message}` to identifier-keyed rich entries, eliminated context_ack.yaml<br>
    **Result**: Seven changes in `orchestrator.py`: added `import re` and `_generate_context_id()` helper (slugification with 37-char truncation, collision suffix `_2`/`_3`, empty fallback to "ctx"). Rewrote `_load_context()` with strict validation - raises `ValueError` on legacy flat strings with "delete context.yaml" instructions. Rewrote `cmd_context()` with 4 modes: add (auto-generates identifier, stores `{message, phase, created, acknowledged_by, processed}`), list (shows rich metadata), clear (by identifier), processed (marks entry done). Rewrote `cmd_start()` banner injection to show message-only for non-processed entries, update `acknowledged_by` inline (no separate `context_ack.yaml`). Rewrote `cmd_status()` to display `[identifier] (PHASE): msg (seen by, processed)`. Added `--processed` argparse flag. Updated `app.yaml` message templates. Replaced 2 old `TestContextAcknowledgment` tests with 11 new `TestContextRichEntries` tests covering id generation, collision, empty fallback, round-trip, legacy rejection, two-messages-same-phase, inline ack, idempotent ack, processed flag. Result: 171 tests pass, lint clean, validate passes, 0 `context_ack` references in orchestrator.py. Benchmark score: 63 (48 unchecked + 15 residual). Also updated PROGRAM.md and BENCHMARK.md with new work items: failures redesign, Occam's razor directive, generative naming, PLAN mirrors EnterPlanMode, resource conflict on version upgrade.

24. **Task - Surgical fixes** (v0.8.51, iter 22): Three surgical fixes identified by forensicist: _load_context key validation, cmd_status created timestamp, hypothesis autowrite prompt<br>
    **Result**: (1) Added required key validation (`message`, `phase`) to `_load_context()` at L739-745 - entries missing either key now raise `ValueError` with descriptive message. Tests `test_entry_missing_message_raises` and `test_entry_missing_phase_raises` confirm. (2) Added `created` timestamp to `cmd_status()` display at L2090-2096, fixed unconditional `...` ellipsis to be conditional on message length. Format now `[id] (PHASE): msg (created: YYYY-MM-DD, ack: X,Y) [PROCESSED]`. (3) Changed `ACTION::HYPOTHESIS_AUTOWRITE` prompt in `workflow.yaml` from "Write entries to hypotheses.yaml" to "Read existing hypotheses.yaml first. APPEND new entries. UPDATE existing by ID. Do NOT overwrite." Test `test_hypothesis_autowrite_prompt_says_append` verifies. Benchmark score: 63 -> 53 (9 items fixed, Format Commitment grade 7->8). 173 tests pass.

25. **Task - Occam directive + gatekeeper context** (v0.8.51, iter 23): Added Occam's razor directive to all architect agents and context awareness to gatekeepers<br>
    **Result**: Three changes to `phases.yaml` and `app.yaml`. (1) Added Occam's Razor directive to all 4 architect agents (FULL::RESEARCH, PLAN, REVIEW, PLANNING::RESEARCH) - each prompt now includes explicit data design simplicity rules: one canonical location per data entity, no shadow copies, no parallel tracking files, challenge "can this be done without a new file?". grep -ci "occam" phases.yaml = 6 matches. (2) Added conditional context check to 5 gatekeeper prompts (RESEARCH, HYPOTHESIS, PLAN, IMPLEMENT, REVIEW): "IF context messages are active, evidence SHOULD reference or acknowledge them." (3) Updated `user_guidance_instruction` in `app.yaml` to include "ACKNOWLEDGE each context message by referencing it in your output". Two new tests. Benchmark score: 53 -> 41 (11 items fixed: S8=3, S13=8, Code Cleanliness grade 7->8). 175 tests pass.

26. **Task - Version check structured YAML** (v0.8.51, iter 24): Converted .version_check from plain text to structured YAML with checked_at timestamp<br>
    **Result**: Rewrote `_check_version()` in `orchestrator.py` L2869-2901. Cache read now parses YAML via `yaml.safe_load`, checks `checked_at` field using `datetime.fromisoformat()` for 24h expiry instead of filesystem `mtime`. Cache write uses `yaml.dump({"latest_version": latest, "checked_at": _now()})`. Legacy plain-text `.version_check` files are silently treated as expired (yaml.safe_load returns str, not dict, so isinstance check fails and cache is refreshed on next PyPI query). Removed unused `import time`. Four tests: `test_version_check_yaml_format` verifies YAML structure, `test_version_check_legacy_plain_text_ignored` confirms str detected, `test_version_check_fresh_yaml_cache` validates datetime comparison, `test_version_check_no_error` confirms silent failure. Benchmark score: 41 -> 35 (5 S10 items fixed, Design Unity grade 7->8). 177 tests pass.

27. **Task - Failures redesign to rich entries** (v0.8.51): Converted failures.yaml from flat list to identifier-keyed rich dict entries, mirroring context.yaml redesign<br>
    **Result**: Thirteen changes across `orchestrator.py` and `test_orchestrator.py`. Renamed `_generate_context_id` to `_generate_entry_id`. Added `_load_failures()`/`_save_failures()`. Rewrote `_append_failure()` with identifier generation and rich fields. Rewrote `_build_failures_context()` with solved/unsolved partitioning. Changed `_count_iteration_failures()` to return (fid, entry) tuples. Updated `cmd_log_failure`, `cmd_failures`, `cmd_status`, `_run_summary`, `cmd_new`. Added `failures.yaml` to `_CLEAN_PRESERVE`. Added failure ack in `cmd_start`. 7 new tests. 184 tests pass.

28. **Task - Resource conflict + NEXT prompt + PLAN labels** (v0.8.51, iter 26): Closed remaining S10b, S12, S15 items plus added S16 action resolution requirements<br>
    **Result**: Three implementation changes. (1) `_detect_stale_resources` with content comparison. (2) NEXT template unsolved failures instruction. (3) PLAN 4-step labels. Benchmark 24->18. 186 tests.

29. **Task - Actions to phases.yaml** (v0.8.51, iter 27): Moved ACTION:: definitions from workflow.yaml to phases.yaml as root-level actions: section<br>
    **Result**: Centralized all phase-related definitions in phases.yaml. Removed `actions:` block from `workflow.yaml`. Added to `phases.yaml` as root-level section. Updated `model.py` _build_actions and reserved keys. All test fixtures updated. Design Unity 10/10. Benchmark 18->7. 186 tests.

30. **Task - PROGRAM.md future enhancements update**: Refined future work items based on user feedback on context lifecycle, schema versioning, prompt quality<br>
    **Result**: Updated PROGRAM.md with three categories of changes. (1) Added **context entry lifecycle** work item: 4-status model (new -> acknowledged -> dismissed/processed) with transition notes recording who/when/why for each status change. Dismissed items hidden from phase banners, processed items track what was added to PROGRAM/BENCHMARK. Replaces the current boolean `processed` flag. (2) Added **prompt quality** items: RESEARCH failure investigation strengthening and gatekeeper context check from SHOULD to MUST for PLAN/IMPLEMENT phases. (3) Marked 7 completed items as [DONE]: resource conflict, hypothesis append, failures redesign, Occam directive, version check YAML, PLAN mirrors EnterPlanMode, strict action resolution. User rejected schema versioning (load errors trigger archive+refresh, no version markers needed) and `--id` flag (identifier always auto-generated, `--message` is fine).

31. **Task - Context lifecycle refactor**: Replaced `acknowledged_by` + `processed` fields with `status` + `notes` lifecycle model in `orchestrator.py`<br>
    **Result**: Nine coordinated changes across `orchestrator.py` and `test_orchestrator.py`. (1) `_load_context` validation now requires `{"message", "phase", "status"}` with `status` validated against `{"new", "acknowledged", "dismissed", "processed"}` - invalid status raises `ValueError`. (2) `cmd_context` ADD mode creates entries with `"status": "new", "notes": []` instead of `"acknowledged_by": [], "processed": False`. (3) `--processed` handler sets `status = "processed"` and appends `{"processed": note_text}` to notes list. (4) New `--dismiss` handler sets `status = "dismissed"` with `{"dismissed": note_text}` note. (5) `cmd_start` ack loop now only transitions `new -> acknowledged` (not touching already acknowledged/processed/dismissed), appending `{"acknowledged": "seen by PHASE"}` note. (6) Banner filter uses `status in {"new", "acknowledged"}` instead of `not processed`. (7-8) `cmd_status` and `cmd_context` list mode display `status` and latest note instead of `acknowledged_by`/`processed`. (9) Added `--dismiss` and `--note` argparse flags to context subparser. Updated 5 tests from old field structure to new lifecycle model. 186 tests pass, ruff format clean.

32. **Task - Hypothesis lifecycle implementation**: Converted hypotheses.yaml from flat list to identifier-keyed dict with status+notes lifecycle, mirroring context.yaml and failures.yaml patterns<br>
    **Result**: Seven changes. _load_hypotheses/_save_hypotheses with validation. _build_context filters deferred+new. _CLEAN_PRESERVE. Autowrite/GC/gatekeeper prompts updated. 7 new tests. 196 tests.

33. **Task - Replace EnterPlanMode** (v0.8.52, iter 30): Removed all EnterPlanMode/ExitPlanMode from PLAN phase templates, replaced with autonomous 4-step planning<br>
    **Result**: Five edits in phases.yaml removing all EnterPlanMode/ExitPlanMode. Autonomous 4-step planning preserved. Benchmark 51->33. 196 tests.

34. **Task - All remaining items** (v0.8.54, iter 32): Failures status+notes migration, context transitions, generative naming, --clean default, gatekeeper enforcement<br>
    **Result**: Fifteen changes across orchestrator.py, phases.yaml, test_orchestrator.py. (1) Failures migrated from acknowledged_by+processed to status+notes, matching context/hypothesis pattern - _append_failure creates with status:"new", cmd_start transitions new->acknowledged, _build_failures_context partitions by status. (2) _VALID_CONTEXT_TRANSITIONS dict enforces terminal states - dismissed and processed cannot transition further. (3) _generate_entry_id accepts optional `identifier` parameter for generative naming - LLM can pass meaningful name, falls back to slugification. (4) --clean default changed from True to False - `orchestrate new` preserves data by default. (5) Five gatekeeper/template updates: NEXT gatekeeper enforces zero new context items, HYPOTHESIS gatekeeper checks notes on non-new items, RESEARCH template instructs unsolved failure investigation, PLAN and IMPLEMENT gatekeepers strengthened from SHOULD to MUST for context reference. Three new tests + six updated. Occam grade 9->10 (all structures consistent). Benchmark 28->19. 199 tests.

35. **Task - --continue flag for new**: Replaced `--clean`/`--no-clean` flags with `--continue` flag on `orchestrate new` command<br>
    **Result**: Three changes across `orchestrator.py`, `app.yaml` (bundled + project-local), `test_orchestrator.py`. (1) Removed `--clean`/`--no-clean` argparse flags, added `--continue` (`dest="continue_session"`) - fresh `new` always cleans artifacts, `new --continue` preserves all data (context, failures, hypotheses, benchmark scores) and increments iteration from existing state. (2) Rewrote `cmd_new` flow: `--continue` path loads existing state (exits with error if none), preserves `benchmark_scores`, increments iteration; fresh path reads last iteration before cleaning then resets. (3) Added `TestNewContinue` class with 3 tests: `test_new_continue_preserves_data` (verifies context survives, iteration increments from 5 to 6, benchmark scores preserved), `test_new_continue_no_state_fails` (SystemExit when no existing session), `test_new_fresh_wipes` (confirms fresh start cleans and resets benchmark_scores to empty). Updated all 6 existing test Namespace references from `clean=False` to `continue_session=False`. Updated `app.yaml` help text in both bundled and project-local copies. 202 tests pass.

36. **Task - Lifecycle compliance gates**: Implemented S1-S4 benchmark items: programmatic status gates, hypothesis auto-dismiss, workflow stop conditions, failure partitioning fix<br>
    **Result**: Four coordinated changes across 6 files. (1) S1 - Added `_check_lifecycle_compliance(phase, state)` function in `orchestrator.py` called from `cmd_end` before gatekeeper. NEXT phase blocks if context entries have `status: new`. HYPOTHESIS phase blocks if hypotheses have `status: new`, warns on missing notes. (2) S2 - Added `hypothesis_max_deferred_iterations: 3` to `app.yaml`, added `config: dict` field to `AppConfig` in `model.py` capturing extra app keys, added `stop_condition: str` to `WorkflowType`. Auto-dismiss logic in compliance check reads max from `_MODEL.app.config`, transitions deferred hypotheses to dismissed after 3 iterations with note. (3) S3 - Added `stop_condition` to all 5 WORKFLOW:: definitions in `workflow.yaml` with benchmark score trajectory stall detection text. Added "Review the stop condition for this workflow before proceeding" to NEXT `template_continue` in `phases.yaml`. (4) S4 - Fixed `_build_failures_context` partition from `status != "processed"` to `status in {"new", "acknowledged"}` for unsolved, aligning with context banner filter style. Five new `TestLifecycleCompliance` tests. Fixed pre-existing ruff issues (unused `time` import, import sorting). 207 tests pass, ruff clean.

45. **Task - Context directive wiring** (v0.8.60): Wired context_acknowledgment_directive into IMPLEMENT, TEST, REVIEW start templates and updated to cover failures<br>
    **Result**: Three fixes to `phases.yaml`. (1) The `&context_acknowledgment_directive` anchor at L57 was defined but never referenced because YAML anchors cannot be interpolated inside literal block scalars (`|`). Instead of using `*context_acknowledgment_directive`, pasted the directive text directly into 3 phase start templates: IMPLEMENT (L619), TEST (L703), REVIEW (L792). (2) Updated the directive text from context-only to "Context & Failure Acknowledgment" covering both context entries and failures - agents must write rich acknowledgment notes for context via `{CMD} context --note` and investigate each failure, with explicit instruction not to leave shallow "seen by" notes. (3) Verified guardian context audit (checklist item 5) already present in both guardian agents - PLAN guardian defines `&guardian_checklist` at L504 and REVIEW guardian references `*guardian_checklist` at L854, both containing the CONTEXT AUDIT item. No other phases have guardian agents. 221 tests pass, model validates with 0 issues.

37. **Task - --restart flag and safety cap**: Added `--restart` flag to `orchestrate new` and made safety cap configurable from app.yaml<br>
    **Result**: Four changes across `orchestrator.py`, `test_orchestrator.py`, and SKILL.md. (1) `--restart` flag shares state-loading path with `--continue` but keeps same iteration number. Phases reset, data preserved, args optionally update objective/benchmark/iterations. (2) Safety cap from config replacing hardcoded 20. (3) SKILL.md --restart documentation. (4) 4 new tests. 212 tests.

38. **Task - Benchmark score 1** (v0.8.58): Final benchmark cleanup across 17 iterations (21-37)<br>
    **Result**: Benchmark score ~95 -> 1 across 17 iterations. Zero unchecked. Format Commitment 9/10 accepted (cache file tradeoff). All other scales 10/10. 212 tests, v0.8.58.

39. **Task - Devils-advocate plugin** (v0.8.58): Added devils-advocate as a full plugin to claude-code-plugins marketplace, refactored into 4 skills<br>
    **Result**: Ported the workspace `devils-advocate` skill from `/home/lab/workspace/.claude/skills/devils-advocate/` to the plugin marketplace as `devils-advocate/` alongside `auto-build-claw/`. Refactored from one monolithic SKILL.md into 4 separate skills: `setup` (build devil persona from seed/description/composite + harvest fact repository), `evaluate` (generate concern catalogue with Fibonacci risk scoring 1-64 + baseline scorecard with 0-100% per concern), `iterate` (two modes: auto-correct from scorecard options OR re-score only when user made corrections themselves - asks which mode first), and `run` (full end-to-end workflow). Setup batches all persona questions in one message instead of asking one at a time. Iterate and evaluate include British-style cheers on significant score improvements ("Jolly good show!"). Updated project README with both plugins listed and Medium article link. Plugin structure: `devils-advocate/skills/{setup,evaluate,iterate,run}/SKILL.md` + `devils-advocate/README.md` with skill table and scoring explanation.

40. **Task - README rewrite planning** (v0.8.59): Wrote PROGRAM.md and BENCHMARK.md for README rewrite, ran devils-advocate analysis, started auto-build-claw planning iteration<br>
    **Result**: Created `PROGRAM.md` with 9 work items organized by iteration target (iter 1: core content, iter 2: architecture, iter 3: polish). Created `BENCHMARK.md` with 31 checklist items across 3 sections (Accuracy, Completeness, Quality) plus 4 fuzzy scales with rubrics. Ran devils-advocate `:run` which identified 7 concerns (total risk 208, residual 121.3): value-first hierarchy missing, no iteration sequencing, no reader experience measurement, no consistency verification, plugin detail too deep for top-level README, no length control, no content audit. Applied corrections: added value-first mandate, explicit iteration targets, fuzzy scale rubrics, path/command verification items, length target (250 lines), link-to-sub-README directive. Auto-build-claw planning iteration created 3-iteration layer-by-layer plan.

41. **Task - README iteration 1** (v0.8.59): Auto-build-claw iteration 1 - rewrote README.md with value-first structure, both plugins documented, install instructions<br>
    **Result**: Complete README.md rewrite from 110 to 162 lines. Opening paragraphs now lead with problem statement ("AI agents cut corners") then solution concept ("YAML-driven orchestration engine with pull-based enforcement") before any technical detail. Added "What it solves" section with 6 concrete problems. Added plugins overview table. auto-build-claw section: concept paragraph, 3 skills listed, 5 workflow types table (full/fast/gc/hotfix/planning), 4 usage examples including `--continue`, link to sub-README. devils-advocate section: concept paragraph, 4 skills with descriptions, Fibonacci risk scoring concept, 3 usage examples, link to sub-README. Install section: pip + marketplace commands. Architecture tree shows both plugin directories (paths need fixing in iter 2 - 4 phantom paths logged as FM-ARCH-PATHS). Development section updated to 212 tests. Benchmark score: ~71 -> 9 (3 unchecked + 6 fuzzy residual). 4-agent review: Critic APPROVE, Architect ISSUES (phantom paths), Guardian BLOCK on architecture (deferred to iter 2), Forensicist identified 7 failures for iter 2 priorities.

42. **Task - README iteration 2** (v0.8.59): Auto-build-claw iteration 2 - fixed architecture paths, rewrote building-a-plugin section, added version<br>
    **Result**: Three targeted fixes to README.md resolving FM-ARCH-PATHS failure. (1) Architecture tree rewritten with 16 verified paths - engine now shows `resources/` subdir with 3 YAML files, both plugins show `.claude-plugin/plugin.json` registration, marketplace `marketplace.json` included. All paths verified via disk check. (2) Building-a-plugin section replaced fictional Python entrypoint (`orchestrate.py` + `resources/`) with actual plugin structure: `SKILL.md` files + `.claude-plugin/plugin.json` registration. (3) Added version 0.8.59 to install section (user later noted badge covers this - removal deferred to iter 3). README 165 lines. Benchmark score: 9 -> 1 (0 unchecked + 1 flow residual at 9/10). 4-agent review: all PASS, forensicist notes only remaining fix is removing explicit version line per user context.

43. **Task - README iteration 3** (v0.8.59): Auto-build-claw iteration 3 (fast) - removed explicit version line per user request, final polish<br>
    **Result**: Removed "Current version: **0.8.59**" from install section per user context that PyPI badge at L4 already provides version information. README reduced from 165 to 163 lines. Benchmark score: 1 -> 0 (all 31 items checked, all 4 fuzzy scales 10/10). Total README rewrite complete across 3 iterations: baseline ~71 -> iter 1 score 9 (value-first rewrite, both plugins) -> iter 2 score 1 (architecture fix, building-a-plugin rewrite) -> iter 3 score 0 (version line removal). Devils-advocate analysis on PROGRAM.md/BENCHMARK.md identified 7 concerns that guided iteration priorities.

44. **Task - Context/Hypothesis/RECORD lifecycle** (v0.8.60): Auto-build-claw iteration 3 - implemented 6 work items for lifecycle enforcement, action execution modes, and RECORD configurability<br>
    **Result**: Ten changes across 4 files (model.py, orchestrator.py, phases.yaml, test_orchestrator.py). (1) `ActionDef` dataclass gains `execution` field ("agent"/"standalone") controlling whether generative actions run in-process or as cold subprocess - addresses hypothesis autowrite never persisting because subprocess had no context. (2) `_run_auto_actions` resolves template variables ({phase_output}, {artifacts_dir}, {iteration}) via format_map before dispatching by execution mode - agent mode prints resolved prompt for main process, standalone calls `_claude_evaluate`. (3) `cmd_start` removed hardcoded thin acknowledgment notes ("seen by PHASE") for both context and failures - agents now responsible for writing rich notes via phase template prompts. (4) `_check_lifecycle_compliance` blocks NEXT phase on "acknowledged" status for both context and failures (previously only blocked "new") - entries must be processed or dismissed before iteration boundary. (5) `_clean_artifacts_dir` gains `preserve_data` parameter - fresh `new` deletes everything except resources/, `--continue` preserves data files. (6) `--record-instructions` CLI flag on `orchestrate new` stored in state. (7) RECORD template conditional on code changes - no commit required when no code modified. (8) ACTION::HYPOTHESIS_AUTOWRITE and HYPOTHESIS_GC get `execution: agent` with template variable prompts. (9) Phase templates get context_acknowledgment_directive anchor and guardian context audit checklist item 5 and NEXT gatekeeper lifecycle check. (10) `_load_state` validates required keys - old state files crash with clear error. 9 new tests + 15 existing tests updated for record_instructions field. 221 tests pass. Benchmark score: ~64 -> 16 (7 unchecked + 9 fuzzy residual).

45. **Task - Context/Hypothesis/RECORD lifecycle iter 4** (v0.8.60): Auto-build-claw iteration 4 (fast) - fixed 7 remaining benchmark gaps<br>
    **Result**: Seven targeted fixes closing all benchmark gaps from iteration 3 forensicist report. (1) Pasted "Context & Failure Acknowledgment" directive text directly into IMPLEMENT (L619), TEST (L703), and REVIEW (L792) start templates in phases.yaml - YAML anchors cannot interpolate into literal block scalars so text was inlined. Covers both context and failure acknowledgment instructions. (2) Added `record_instructions` to `_build_context()` ctx dict at orchestrator.py L480 - the {record_instructions} template variable in RECORD template now resolves from state instead of silently rendering empty. (3) Guardian context audit (checklist item 5) already present in all guardians via &guardian_checklist anchor - no change needed. (4) Added `TestStandaloneActionDispatch` with mock `_claude_evaluate` verifying standalone execution path resolves template vars and calls subprocess. (5) Added `TestRecordPhaseTemplate` asserting RECORD template contains conditional commit language ("If NO code changes") and {record_instructions} placeholder. (6) SKILL.md updated with new command flags table documenting all 8 `orchestrate new` flags including `--record-instructions`. (7) Fix 7 merged with Fix 1 - directive covers failures alongside context. Benchmark score: 16 -> 0 (all items pass, all fuzzy scales 10/10). 224 tests pass.

46. **Task - Research/hypothesis/file placement** (v0.8.61): Auto-build-claw iteration 6 - research output depth, hypothesis richness enforcement, output file placement, notes validation<br>
    **Result**: Seven changes across 4 files. (A) phases.yaml RESEARCH template gains mandatory 4-section output structure (Current State Summary, Gap Analysis, File Inventory, Risk Assessment) with per-finding format (FINDING/EVIDENCE/IMPACT/FILES). Gatekeeper replaced with structural FAIL checks - missing sections or absent code references fail. (B) phases.yaml HYPOTHESIS gatekeeper rewritten with per-field format checks (ID/HYPOTHESIS/WHAT TO DO/PREDICT/EVIDENCE/RISK/STARS) - FAIL if any selected hypothesis missing any field. ACTION::HYPOTHESIS_AUTOWRITE prompt specifies notes dict format and valid statuses. (C) All 8 `--output-file "path/to/X.md"` placeholders replaced with `{phase_dir}/X.md` - zero path/to/ references remain. (D) orchestrator.py `_validate_end_inputs` resolves relative --output-file paths against `_phase_dir(state)` via is_absolute() check instead of CWD. (E) orchestrator.py `_build_context` gains `phase_dir` variable from `_phase_dir(state)`. (F) model.py `_KNOWN_VARS` gains `phase_dir`. (G) orchestrator.py `_load_hypotheses` validates notes are list of dicts with status key - plain string notes crash with ValueError. Invalid status "selected" also crashes. 6 new tests (structural checks, format fields, file resolution, notes validation, status validation, context var). 230 tests, lint clean. Benchmark ~56 -> 0 in one iteration.

47. **Task - Program/benchmark writer rewrite** (v0.8.62): Rewrote program-writer and benchmark-writer skills as iterative dialogues with intention extraction<br>
    **Result**: Complete rewrite of both companion skills. `program-writer/SKILL.md`: changed from single-shot generation to multi-round dialogue. Round 1 asks 6 questions (end state, current state, why it matters, off-limits, measurability, risk) with follow-up to separate intention from implementation. Round 2 proposes draft. Round 3+ iterates on user feedback until explicit approval. Work items now grouped by logical category (not iterations - PLANNING phase handles that) with predict (from X to Y) and outcome (what user gets) fields alongside scope and acceptance. Expert knowledge (architectural constraints, technology choices, design decisions) explicitly captured as domain guidance, not implementation detail. Program can be comprehensive - no one-screen constraint. `benchmark-writer/SKILL.md`: pushes hard for programmatic metrics over subjective checklists. Metric hierarchy: command output > file checks > computed metrics > binary checklists > fuzzy scales (last resort, must justify why not programmatic). Every fuzzy scale requires "why not programmatic?" justification. Multi-round dialogue with user on targets and weights. README updated with simplified seed-prompt usage pattern. Plugin versions bumped to 1.0.1. `/increment-version` command added.

48. **Task - Fix real gaps iteration 1** (v0.8.63): Fixed broken subsystems identified by honest assessment - path doubling, agent no-op, rate limits, CLAUDE.md readback<br>
    **Result**: Nine changes addressing actual runtime failures (not prompt theater). (1) `_claude_evaluate` rate limit retry: detects "hit your limit"/"rate limit"/"too many requests" patterns, retries 3x with exponential backoff (5s/15s/45s), logs each retry. (2) Fresh `orchestrate new` iteration counter: always starts at 1 (was reading old log.yaml before clean, carrying over stale iteration numbers). (3) Removed `execution: agent` stdout print from `_run_auto_actions` - was a no-op that printed to stdout and hoped someone would read it. All generative actions now use `_claude_evaluate` with resolved template vars. (4) `_validate_research_output`: programmatic validation of RESEARCH output - checks required section headers, token count, file path references. Called from `cmd_end` for RESEARCH phase, sys.exit(1) on failure. (5) `_validate_hypothesis_richness`: per-entry token-based validation - hypothesis, prediction, evidence fields checked against configurable floors from app.yaml. (6) `_phase_dir` returns absolute path via `.resolve()` - fixes the critical path doubling bug. (7) RECORD template + readback: CLAUDE.md always wins. (8) HYPOTHESIS end template instructs agent to write hypotheses.yaml before calling end. (9) Removed `execution` field from ActionDef. Added `artifacts_dir` to `_build_context` and `_KNOWN_VARS`. 6 new tests. 236 tests, lint clean. Benchmark ~45 -> 7.

49. **Task - Fix real gaps iterations 2-3** (v0.8.65): Wired hypothesis richness into compliance, PLAN gatekeeper checklist, tiktoken quality floors, phase-as-manager pattern<br>
    **Result**: Iteration 2 (fast): wired `_validate_hypothesis_richness` call into `_check_lifecycle_compliance` for HYPOTHESIS phase - entries failing richness checks now cause sys.exit(1) at phase boundary. Deferred entries correctly skipped alongside dismissed. Iteration 3 (fast): three major enhancements. (1) PLAN gatekeeper replaced with rigorous 7-item checklist: specific files, root causes, acceptance criteria, risk assessment, predictions with metrics, 2-3 alternatives explored with justification, review agents challenged the plan. FAIL on any missing/vague item. (2) Shared PLAN template gains "explore 2-3 alternative approaches, have contrarian challenge, select with justification" instruction - now in all plan phases. Plan must be grounded in hypotheses from HYPOTHESIS phase. (3) tiktoken dependency added for proper token counting. All quality floors moved to app.yaml (configurable per plugin): research_min_tokens: 500, research_section_min_tokens: 50, research_min_file_refs: 5, hypothesis_min_tokens: 50, prediction_min_tokens: 30, evidence_min_tokens: 30. (4) Research agent prompts enriched - all 3 agents now require structured findings (FINDING/EVIDENCE/IMPACT/FILES) with minimum counts (5, 3, 3). (5) Established the **phase-as-manager pattern**: initially added a 4th research_manager agent to review merged output, but this added overhead without proportional value. Instead, the pattern is that the phase itself (the orchestrating agent) IS the manager. It dispatches specialist agents as tools, synthesizes their output (cross-referencing, resolving contradictions, filling gaps), and self-evaluates against a downstream readiness checklist before releasing. The manager role is: own output quality, not just dispatch and dump. If agents bring shallow findings, send them back. If there are gaps between what agents found and what downstream phases need, fill them. This pattern applies to any phase with specialist agents - RESEARCH, HYPOTHESIS, PLAN, REVIEW all follow the same structure: phase owns quality, agents are tools, synthesis is the manager's job. Rate limit retry confirmed working in production. Benchmark ~45 -> 7 -> 2 -> 1. 236 tests.

50. **Task - Session summary** (v0.8.65, plugins v1.0.2): Comprehensive session covering README rewrite, lifecycle enforcement, real gap fixes, quality enforcement, and architectural patterns<br>
    **Result**: This session delivered 5 major workstreams across ~20 orchestrator iterations, taking the project from v0.8.59 (212 tests) to v0.8.65 (236 tests). **Workstream 1 - README rewrite** (entries 40-43): devils-advocate analysis identified 7 concerns in PROGRAM/BENCHMARK, then auto-build-claw ran 3 iterations rewriting README.md from 110 to 163 lines with value-first framing, both plugins documented, verified architecture paths, benchmark ~71 -> 0. **Workstream 2 - Context/hypothesis/RECORD lifecycle** (entries 44-45): forensic analysis of prior session revealed context stuck at "acknowledged", hypotheses.yaml empty, RECORD forcing unnecessary commits. Fixed: lifecycle compliance blocks acknowledged at NEXT boundary, ActionDef.execution field for agent dispatch, --record-instructions CLI flag, RECORD conditional on code changes, _clean_artifacts_dir conditional preserve, _load_state validates required keys. 212 -> 224 tests. **Workstream 3 - Research/hypothesis output quality** (entry 46): RESEARCH template gains mandatory 4-section structure, HYPOTHESIS gatekeeper checks all 7 format fields, output-file paths use {phase_dir}, _load_hypotheses validates notes format. 224 -> 230 tests. **Workstream 4 - Fix real gaps** (entries 48-49): honest assessment revealed prior "fixes" were prompt theater (template text presence, not runtime behavior). Fixed for real: `_claude_evaluate` rate limit retry with 3x exponential backoff (confirmed working in production), `_phase_dir.resolve()` fixing critical path doubling bug, RECORD readback respects CLAUDE.md (always wins), HYPOTHESIS end template replaces broken stdout auto-action, `_validate_research_output` programmatic validation (500 tokens, 4 sections, 5 file refs), `_validate_hypothesis_richness` token-based validation (hypothesis 50, prediction 30, evidence 30 tokens), fresh `orchestrate new` always starts at iteration 1, removed dead `execution: agent` mechanism. 230 -> 236 tests. **Workstream 5 - Quality enforcement architecture** (entry 49 continued): PLAN gatekeeper replaced with 7-item checklist matching EnterPlanMode rigor. tiktoken added for proper token counting. All quality floors configurable in app.yaml. Research agents enriched with structured finding format (FINDING/EVIDENCE/IMPACT/FILES). Established the **phase-as-manager pattern**: the orchestrating agent IS the manager of each phase - it dispatches specialist agents as tools, synthesizes their output (cross-referencing, resolving contradictions, filling gaps), and self-evaluates against a downstream readiness checklist (hypothesis readiness, plan readiness, implementation readiness, gap check) before releasing. This pattern applies universally: RESEARCH, HYPOTHESIS, PLAN, REVIEW all follow the same structure where the phase owns quality, agents are tools, and synthesis is the manager's responsibility. **Companion skills rewritten** (entry 47): program-writer now iterative dialogue extracting intention (not implementation), capturing expert knowledge, logical grouping with predict/outcome/depends-on fields, exit conditions tied to benchmark score (stagnation/target/scope completion - asks user explicitly). benchmark-writer pushes programmatic metrics over subjective checklists with metric hierarchy and "why not programmatic?" justification for fuzzy scales. **Other fixes**: `/increment-version` command, argument-hint changed from CLI flags to seed prompt, `{{status: "message"}}` brace escaping in templates, `{artifacts_dir}` in template context. Plugin versions bumped to 1.0.2. Published v0.8.65 to PyPI.

51. **Task - Session summary + duplicate output fix** (v0.8.66, plugins v1.0.3): Added session summary display on `orchestrate new` and fixed duplicate output files in phase directories<br>
    **Result**: Two fixes. (1) Session summary: added `session_summary` message template to `app.yaml` with box-drawing border. `cmd_new` in `orchestrator.py` prints compact summary block before "To begin" showing: objective (truncated at 50 chars), workflow type + phase count, iterations (or "unlimited" with safety cap), benchmark yes/no with first 50 chars, and session type (fresh start / continuing from iter N). Template uses `{summary_objective}`, `{summary_workflow}`, `{summary_iterations}`, `{summary_benchmark}`, `{summary_session}` variables populated from state dict. (2) Duplicate output fix: `_record_phase_outputs` was unconditionally copying output-file content to `output.md` in the phase directory even when the output-file was already inside that directory - creating two identical files (e.g. `research.md` + `output.md`). Fixed by checking `str(output_file_path).startswith(str(pdir))` before creating `output.md`. Now creates `output.md` only as fallback when the output-file is outside the phase directory or when only evidence was provided (no --output-file). Also in this session: benchmark-writer clarified as scalar evaluation function only (no exit conditions - those belong in PROGRAM.md), data science metrics added to metric hierarchy (MSE, RMSE, F1, KL divergence, correlation coefficients), program-writer exit conditions now tied to benchmark score (stagnation/target/scope). Plugin versions bumped to 1.0.3. Published v0.8.66 to PyPI.

52. **Task - Note richness enforcement** (v0.8.66): Added programmatic note richness check - every context/failure note must be >= 50 tokens<br>
    **Result**: Added `_check_note_richness(phase)` function called from `_check_lifecycle_compliance` at every phase end (not just NEXT). For every context and failure entry with status "acknowledged", "dismissed", or "processed", the latest note must have >= 50 tokens measured by tiktoken. This closes the last enforcement gap: agents could previously transition an entry's status (e.g. acknowledged -> processed) with a thin note like "seen by PLAN" (3 tokens) and pass all gates. Now that note would hard-fail with `sys.exit(1)` naming the entry and token count. The threshold is configurable via `note_min_tokens: 50` in `app.yaml` alongside the other quality floors (research_min_tokens, hypothesis_min_tokens, etc.). Test fixtures updated with rich notes that meet the 50-token minimum - the notes now read as genuine audit trail entries explaining what was done, which agents referenced the entry, and what impact it had. Zero residuals remaining across all enforcement areas.

53. **Task - Devils-advocate improvements** (v0.8.67, plugins v1.0.4): Added improve skill, run command, differentiator, redesigned iterate flow<br>
    **Result**: Five changes to the devils-advocate plugin. (1) New `improve` skill bridging evaluate and iterate - asks user how to address top concerns with three modes: user suggestions (you tell me what to change), auto-apply (AI applies recommended options from scorecard), or planning mode (discuss each concern and design approach together). Can mix modes per concern. (2) `commands/run.md` added so `/devils-advocate:run` appears in autocomplete - previously only registered as a skill, invisible in `/` command list. Run command redesigned as setup -> evaluate -> improve/iterate loop that repeats until residual risk < 10% or stagnation. (3) `iterate` skill redesigned with clear two-mode behavior: when AI makes changes, always creates versioned file `<name>_v<NN>_<score>.md` with embedded scorecard; when user makes changes outside Claude, re-scores in place and updates `devils_advocate.md` only without creating versioned copy. (4) Differentiator paragraph added to README comparing against qualitative tools (mattpocock's grill-me for Socratic interview, mcpmarket's Devil's Advocate Protocol for one-shot adversarial reasoning) vs our semi-data-science approach: custom persona deviously inferred from existing conversations/emails/transcripts, Fibonacci risk scoring (1-64), measurable residual per iteration, score trajectory showing convergence, versioned audit trail. (5) Plugin now has 5 skills (was 4): setup, evaluate, improve, iterate, run. Published v0.8.67 to PyPI, plugins v1.0.4.

54. **Task - CI/CD, benchmark execution recipes, note enforcement** (v0.8.68, plugins v1.0.6): Restructured CI into proper stages, added benchmark execution recipes, note richness enforcement, pyproject URLs<br>
    **Result**: Six changes. (1) CI restructured from monolithic job into 5 stages: lint (ruff, fast), test (pytest + coverage + YAML validation), build (wheel, depends on lint+test), test-install (install from wheel artifact, verify imports + CLI), check-links (lychee on READMEs, excludes medium.com/pepy.tech that return 403). Fixed `PhaseFSM` -> `FSM` import in test-install. (2) Benchmark-writer gains execution recipes: every check must have an `Execution:` line with exact command/script/procedure. New scenario tests section for complex checks - Playwright browser tests, API tests, simulation runs, generative scenario tests with prompt templates. Rule added: "no ambiguous 'verify X works' - show the command". (3) Note richness enforcement via `_check_note_richness(phase)` - runs at every phase end, every acknowledged/dismissed/processed context/failure entry must have latest note >= 50 tokens (configurable `note_min_tokens` in app.yaml). Catches "seen by PLAN" (3 tokens). (4) `pyproject.toml` gains `[project.urls]` with Homepage, Repository, Issues links to GitHub. Fixed TOML section ordering (urls must come after all project keys). (5) Benchmark-writer clarified as scalar evaluation function (no exit conditions), data science metrics added to hierarchy, program-writer exit conditions tied to benchmark score. (6) Session summary box-drawing display on `orchestrate new` + duplicate output file fix in `_record_phase_outputs`. Published v0.8.68, plugins v1.0.6.

55. **Task - Datascience plugin** (v0.8.69, plugins v1.0.7): Added datascience plugin with notebook standards, rich styling, copier scaffolding, compliance fixes, and prompt engineering<br>
    **Result**: New datascience plugin derived from workspace standards (DATASCIENCE.md, NOTEBOOK.md, RICH.md) plus AGENT_PROMPT.md. 6 auto-triggered skills: `datascience` (project conventions, naming, PyTorch, Polars), `notebook-standards` (section order, GPU-first, imports), `rich-output` (semantic colors, tables, ML evaluation), `prompt-engineering` (7 research-backed techniques in separate reference files), `progressbars` (tqdm classic and rich modern styles, Jupyter compatibility), `journal` (append-only entry management with verification, archiving at 40 entries, examples in `references/examples.md`). 10 commands: `/datascience:new-project` (copier scaffold), `/datascience:notebook` (structured Jupytext), `/datascience:review` (compliance checklist), `/datascience:apply-style` (fix rich colors and print patterns), `/datascience:apply-progressbar` (add/fix progress bars - choose tqdm or rich), `/datascience:apply-prompt-technique` (choose from 7 techniques with stacking), `/datascience:fix-notebook` (restructure to standards), `/datascience:fix-project` (port to copier template - 3 modes), `/datascience:challenge` (full psychological stack for hard problems), `/datascience:archive-journal` (archive older entries, keep last 20). Auto-build-claw `--type` now defaults to `fast` (no longer required). Fix commands are destructive (apply changes directly, ask critical questions). Marketplace: 3 plugins. Plugins v1.0.8. Published v0.8.69.

56. **Task - Plugin version bump** (plugins v1.0.9): Bumped all plugin versions from 1.0.8 to 1.0.9<br>
    **Result**: Updated 7 version strings across 4 files: `marketplace.json` (metadata + 3 plugin entries), `auto-build-claw/.claude-plugin/plugin.json`, `devils-advocate/.claude-plugin/plugin.json`, `datascience/.claude-plugin/plugin.json`. All plugins synchronized at v1.0.9.

57. **Task - Journal plugin extraction** (v0.8.71, plugins v1.0.10): Extracted journal from datascience into standalone plugin with create, update, and archive commands<br>
    **Result**: Created `journal/` as the 4th plugin in the marketplace. Moved journal skill and references from `datascience/skills/journal/` to `journal/skills/journal/` with identical SKILL.md and `references/examples.md`. Created 3 commands: `create` (append new entry with auto-numbering and level detection), `update` (edit most recent entry with corrections or additional detail), `archive` (move older entries to JOURNAL_ARCHIVE.md keeping last 20). Removed `datascience/skills/journal/` directory and `datascience/commands/archive-journal.md`. Updated `marketplace.json` with 4th plugin entry. Updated main `README.md` with 4-plugin table, new journal section with usage examples, updated architecture tree showing journal plugin structure, datascience reduced to 5 skills and 9 commands. Updated `datascience/README.md` removing journal references. Updated `increment-version.md` for 4 plugin.json files. Bumped pyproject.toml to v0.8.71 and all plugin versions to v1.0.10.

58. **Task - Notebook/fix-notebook enrichment** (v0.8.72, plugins v1.1.1): Updated notebook and fix-notebook commands to apply styling, progress bars, narrative headers, and manage deps<br>
    **Result**: Forensic analysis of the audio-transcription project session revealed 4 manual fix passes on a single notebook (structure reorder, rich styling conversion, progress bar addition, API fixes) that should have been one-pass automated. Updated four datascience commands. Refactored to eliminate rule duplication: commands now reference skills (`notebook-standards`, `rich-output`, `progressbars`) as single source of truth instead of inlining color palettes and patterns (99 lines added, 261 removed). (1) `/datascience:notebook` reads 3 skills before scaffolding, asks about purpose (narrative header), GPU, libraries, long-running ops, progress bar style. Default `.ipynb`, respects `.py` if user prefers. (2) `/datascience:fix-notebook` reads 3 skills, fixes header narrative, section order, styling, progress bars, pyproject.toml deps. Never renames or converts formats. (3) `/datascience:apply-style` reads `rich-output` skill only - no duplicated palette. (4) `/datascience:apply-progressbar` reads `progressbars` skill, updates pyproject.toml. Design principle: commands say WHAT to fix, skills say HOW. One place to update rules, all commands pick it up.

59. **Task - Benchmark exit condition forensics** (v0.8.73, plugins v1.1.2): Forensic analysis of court-cases project revealed benchmark-writer bypass, strengthened anti-patterns, data science metric weighting<br>
    **Result**: Forensic analysis of the court-cases/2025-12_pozb_wladzy_rodz session (22MB JSONL) revealed the #1 cause of malformed benchmarks: the benchmark-writer skill was NEVER invoked. BENCHMARK.md was written directly by the main assistant via Write tool at 10:34:10, including an "## Exit Conditions" section with "Stop iterating when ANY is true: 1. Composite >= 0.70, 2. RQI improvement < 0.02..." - user caught and removed it within 1 minute at 10:35:14. The benchmark-writer skill has safety rules preventing exit conditions, but they were never applied because the skill was bypassed. Three fixes: (1) `benchmark-writer/SKILL.md` anti-patterns massively strengthened - 5 specific WRONG examples (iterations stop when, completion conditions section, stop when score reaches, exit when all items pass, convergence checks disguised as checklist items), self-interrupt instruction ("if you catch yourself writing stop/exit/completion - STOP"), HTML comment block in template structure preventing section addition after iteration log. Intro section now makes the mistake the FIRST thing Claude reads. (2) `run.md` command adds structural enforcement: "NEVER write BENCHMARK.md directly with Write tool. Always invoke benchmark-writer skill." This is the layer that was missing - the skill bypass. (3) Data science metrics now explicitly carry highest weight (>= 50%) in benchmark formula. Weight hierarchy codified: data science metrics > programmatic checks > binary checklist > fuzzy scales. DS metrics are the optimisation target; checklists are guardrails. The court-cases benchmark (RQI 60%, checklist 40%) already followed this pattern - now it's the default rule.


60. **Task - Plugin version bump** (plugins v1.1.3): Bumped all plugin versions<br>
    **Result**: Updated all 5 plugin.json files + marketplace.json (metadata + 5 plugin entries) from 1.1.2 to 1.1.3.

61. **Task - Hypotheses.yaml rescue + prompt fix** (v0.8.73): Fixed hypotheses.yaml written to wrong location, added rescue mechanism and clarified template<br>
    **Result**: Forensic analysis of henryk/court-cases/2025-12_pozb_wladzy_rodz session revealed why `hypotheses.yaml` was empty: the agent wrote it to `phase_02_hypothesis/hypotheses.yaml` (the phase directory) instead of `.auto-build-claw/hypotheses.yaml` (artifacts root). `_load_hypotheses()` reads from artifacts root, found empty `{}`, passed all compliance checks silently. Root cause: HYPOTHESIS END template mentions both `{artifacts_dir}/hypotheses.yaml` and `{phase_dir}/hypotheses.md` - agent confused the two paths and wrote the YAML to the phase dir alongside the MD. Two fixes: (1) `_check_lifecycle_compliance` now rescues the file before validation - if root `hypotheses.yaml` is empty `{}` but the phase dir has a real one with entries, copies it over with "Rescued hypotheses.yaml from phase_NN/ to artifacts root" message. (2) HYPOTHESIS END template rewritten to explicitly state TWO separate write locations with **"IMPORTANT: {artifacts_dir} and {phase_dir} are DIFFERENT directories"** callout. The .yaml goes to artifacts root (persistent catalogue), the .md goes to phase subfolder (phase output). Both prompt fix (prevent confusion) and rescue mechanism (recover when it happens anyway) in place.

62. **Task - Devils-advocate real-world example** (v0.8.74, plugins v1.1.4): Added full reference example from knowledge-graph-foundry analysis<br>
    **Result**: Added two example files to `devils-advocate/examples/`: (1) `kg-builder-design-analysis.md` - condensed devil persona card with 2 representative concerns (#1 agent overuse at risk 20, #5 missing confidence model at risk 25) showing Their Take / Reality / Response narrative pattern, plus 6-iteration score trajectory table (88.9 -> 15.5). (2) `kg-builder-full-analysis.md` - complete 283-line analysis from the knowledge-graph-foundry DESIGN.md review: senior backend engineer persona with all 10 concerns (risk 6-25), full narrative per concern, and 6 versioned scorecards (v1 through v6) showing per-concern score evolution across iterations. README updated with links to both examples and summary table showing math-driven priority ordering. Also added `document-processing` and `journal` plugins to marketplace in prior session. 5 plugins total: auto-build-claw, devils-advocate, datascience, journal, document-processing. Published v0.8.74, plugins v1.1.4.


63. **Task - Score suffix enforcement** (plugins v1.1.4): Strengthened mandatory `_<score>` suffix on all versioned devils-advocate files<br>
    **Result**: Strengthened score suffix requirement across 4 files in the devils-advocate plugin. `iterate/SKILL.md`: added "MANDATORY: Score suffix on every versioned file" header with 5-step process (embed scorecard, calculate residual, rename with suffix, example progression, incompleteness warning). `improve/SKILL.md`: both Mode 1 (user suggestions) and Mode 2 (auto-apply) now explicitly note "score suffix added by iterate after re-scoring" and "hand off to iterate which will rename to `<name>_v<NN+1>_<score>.md`". `commands/run.md`: versioned files section strengthened with "MANDATORY" label, concrete score examples (`_v01_89.md`, `_v02_34.md`, `_v03_12.md`), and "file without score suffix is incomplete" warning. `skills/run/SKILL.md`: file naming convention gains "MANDATORY" on the score field and explicit WRONG example: "`DESIGN_v02.md` (missing score suffix - how improved is it? Nobody knows without opening the file)". The score in the filename IS the audit trail - you can see improvement at a glance from directory listing.

64. **Task - Devils-advocate workflow redesign** (v0.8.74, plugins v1.1.6): Scoring mode choice, iterate absorbs improve, evaluate creates v01, every scoring embeds + renames<br>
    **Result**: Three changes to the devils-advocate plugin. (1) `evaluate/SKILL.md` gains execution mode choice: ASK user whether to score in-session (see reasoning live, calibrate persona, challenge scores) or standalone (`claude -p`, faster for re-scoring iterations). Added Step 6: evaluate now creates `<name>_v01.md` with embedded scorecard and renames to `<name>_v01_<score>.md` - previously evaluate only appended to `devils_advocate.md` and the target document was never scored as a versioned file. The original document is NOT modified. (2) `iterate/SKILL.md` completely rewritten as clear 6-step workflow absorbing the improve skill's functionality. Step 1: decide how to improve (user suggestions / auto-apply / planning mode / user already edited). Step 2: apply changes + create `<name>_v<NN>.md` (no score suffix yet). Step 3: score (in-session or standalone). Step 4: embed scorecard at bottom of versioned file + RENAME to `<name>_v<NN>_<score>.md`. Step 5: update `devils_advocate.md`. Step 6: report + check stopping criteria. The key principle: every scored file carries its own scorecard inside and its residual in the filename. (3) `improve/SKILL.md` simplified to redirect - all functionality now in iterate's Step 1. Run command updated to reflect: setup -> evaluate -> iterate loop. Plugins bumped to v1.1.6.

65. **Task - SVG infographics plugin** (plugins v1.1.7): Imported svg-infographics skill as 6th marketplace plugin with full codebase, validation tools, 64 anonymised examples, and tests<br>
    **Result**: Created `svg-infographics/` plugin from the global `~/.claude/skills/svg-infographics/` skill. Split the monolithic 1400-line SKILL.md into 4 focused skills: `svg-standards` (core rules - grid layout, CSS theme classes, dark/light mode, cards, arrows, connectors, typography, icons, z-order), `workflow` (mandatory 6-phase sequential process with gate checks per image - research, invisible grid, scaffold, content, finishing, validation), `theme` (swatch generation, palette approval, colour naming, brand management), `validation` (5 Python checker tools usage, fail-first verification workflow, browser testing). Copied 5 Python validation tools to `tools/`: `calc_connector.py` (diagonal connector geometry calculator with SVG snippet output), `check_overlaps.py` (inner/outer bounding box overlap detection), `check_contrast.py` (WCAG 2.1 AA/AAA contrast with light/dark mode), `check_alignment.py` (grid snapping, vertical rhythm, topology verification), `check_connectors.py` (zero-length, edge-snap, L-routing, label clearance). Copied 64 SVG examples and anonymised all client names: DeLaval -> Meridian, Nordea -> Apex Financial, Atlas Copco -> Titan Industries, Perfekta -> Optima Manufacturing, KGF -> Nexus; kept Kolomolo and Stellars-Tech as the author's own brands. Created 5 commands: `create` (full 6-phase workflow), `theme` (swatch generation/approval), `validate` (run all checkers), `fix-style` (CSS/contrast/dark mode fixes), `fix-layout` (overlap/alignment/spacing fixes). Added mandatory task tracking (TaskCreate/TaskUpdate) requirement to all 4 svg-infographics skills and all 4 devils-advocate skills. Wrote `tests/test_svg_tools.py` with 51 tests covering calc_connector (horizontal, vertical, diagonal, margin, cutout modes), check_contrast (CSS parsing, luminance, blend, colour resolution, WCAG thresholds), check_connectors (point-to-segment distance, parsing), check_overlaps (simple, overlap detection, inject/strip bounds), check_alignment (rhythm, grid, tolerance), smoke tests on real examples, plugin structure validation, and anonymisation verification. Updated marketplace.json (6 plugins now), root README with svg-infographics section and architecture tree. All 287 tests passing. Plugins bumped to v1.1.7.

66. **Task - CI link checker fix** (plugins v1.1.7): Fixed lychee link checker failures in CI by excluding private repo and rate-limited host<br>
    **Result**: Investigated CI failures across the last 5 runs - all jobs (lint, test, build, test-install) passed but `check-links` consistently failed. Lychee log showed two link errors in `devils-advocate/README.md`: (1) `github.com/stellarshenson/knowledge-graph-foundry` returned 404 because the repo is private and lychee runs unauthenticated against GitHub, (2) `mcpmarket.com/tools/skills/devil-s-advocate-protocol` returned 429 from CI runners due to rate limiting. Both URLs are valid - the fix was to add them to the lychee `--exclude` list in `.github/workflows/ci.yml`. Verified the private repo exists via authenticated GitHub API call. New CI run on commit `c5b12c4` completed successfully.

67. **Task - README plugin descriptions rewrite** (plugins v1.1.7): Rewrote all 6 plugin one-line descriptions in the README plugin table to be value-focused rather than problem-focused<br>
    **Result**: All plugin descriptions in the README plugin table changed from "problem - solution" framing to "what it produces" framing per user direction. Updated entries: `auto-build-claw` now describes executing code and artefact builds toward an objective with benchmark-driven iterations; `devils-advocate` describes producing high-quality documents for a specific audience using a scientific, measured, iterative approach; `svg-infographics` describes producing high-quality standardised SVG infographics with grid-first design and automated checkers; `datascience` describes producing high-quality data science projects and notebooks following consistent standards; `document-processing` describes processing documents according to user requests with grounding in source materials; `journal` describes producing a work journal marking key changes, implementations, and decisions. The new descriptions explain what the plugin produces and how it operates rather than what problems it avoids.

68. **Task - Footnotes skill** (plugins v1.1.8): Added footnotes skill and apply-footnotes command to datascience plugin<br>
    **Result**: Jupyter markdown and many renderers don't support standard `[^1]` footnotes. Added `footnotes` skill and `/datascience:apply-footnotes` command using HTML-compatible anchor pattern that works in JupyterLab, GitHub, GitLab, and standard markdown: in-text `[<sup>N</sup>](#fnN)` superscript links with `<span id="fnN"><sup>N</sup> content</span>` at bottom. The skill auto-triggers when adding references, citations, notes, or footnotes in any markdown context (notebooks and markdown files). The command applies footnotes to existing files: converts inline references to superscript links, creates footnote sections, fixes broken `[^1]` syntax (not supported in Jupyter), renumbers mismatched references. IDs must be unique across the notebook (`fn1`, `fn2`... or descriptive `fn_dataset`, `fn_paper`). Footnotes placed after `---` separator either in the same cell or in a dedicated `## Footnotes` cell. Datascience plugin now has 6 skills and 10 commands.

69. **Task - Footnotes skill rewrite from production** (plugins v1.1.9): Rewrote footnotes skill with proven JupyterLab-compatible pattern from timeline-communication.md<br>
    **Result**: The original footnotes skill pattern didn't work reliably in JupyterLab - learned from production use in `henryk/court-cases/2025-12_pozb_wladzy_rodz/3-output/timeline-communication.md` which has 100+ working footnotes. Rewrote `datascience/skills/footnotes/SKILL.md` with the proven pattern. Key findings: (1) `<span id="D005">` is the only reliable target element - JupyterLab sanitizer renames `id` to `data-jupyter-id` but the click handler still matches via `scrollIntoView()`. `<div>` and heading tags don't work reliably. (2) Inline reference uses `[<sup>D005</sup>](#D005)` with hash link - renders as clickable blue superscript. (3) Target uses `<span id="D005">D005 \`path/to/file.pdf\`</span>` in a bullet point - renders with monospace path. (4) No `<br>` needed between bullet entries. (5) `<sup>` must be inside the inline link only, NOT in the target span. Added "How it works in JupyterLab" section explaining the sanitizer -> data-jupyter-id -> scrollIntoView chain. Added numbering schemes table (D001 for evidence, fn1 for footnotes, ref1 for citations). Added "Common mistakes" section documenting all failures encountered in production: div instead of span, sup in target, file paths instead of hash links, missing hash prefix.

70. **Task - Plugin version bump** (plugins v1.1.10): Bumped all plugin versions<br>
    **Result**: Updated all 6 plugin.json files + marketplace.json (metadata + 6 plugin entries) from 1.1.9 to 1.1.10.

71. **Task - Devils-advocate examples** (plugins v1.1.10): Added executive pushback and README rewrite as reference examples<br>
    **Result**: Added two new examples to `devils-advocate/examples/` alongside the existing kg-builder pair. (1) `executive-pushback-analysis.md` - the richest example with 21 concerns across 8 iterations (score 269 -> 2). VP persona who reads tone before facts. Shows evolving concern catalogue where new concerns (#13-#21) were added as the document improved and revealed deeper issues. Includes a positive concern (#15 SOW quotes reduce total risk by -25). Tone and structure concerns dominate: "this reads like excuses" (#8), "I stopped reading after the third percentage" (#10/#14), "you are pointing the finger at us" (#12), "verbose sentences signal lack of confidence" (#16). Final iteration solved the numbers problem with SVG infographics replacing inline percentages. Client anonymized (Arelion -> Meridian Networks). (2) `readme-rewrite-analysis.md` - from earlier session's devils-advocate run on PROGRAM.md + BENCHMARK.md for the README rewrite. Senior dev "skimmer" persona: "reads first paragraph, scans headers, confused after 30 seconds moves on." 7 concerns with recommended corrections. Shows a different use case: evaluating program/benchmark quality before running the orchestrator, not evaluating a final document. README examples table reordered by richness: executive pushback first (21 concerns, 8 iterations), README rewrite second (7 concerns, baseline), kg-builder third (10 concerns, 6 iterations).

72. **Task - PyPI v1.0 release** (v1.0.5): Published v1.0.5 to PyPI as the first 1.x release, removed stale PROGRAM.md and BENCHMARK.md<br>
    **Result**: Set pyproject.toml version to 1.0.4, `make publish` auto-incremented to 1.0.5 and published. Removed `PROGRAM.md` and `BENCHMARK.md` which were leftover from the auto-build-claw README rewrite session and no longer relevant. 287 tests passing. Plugins at v1.1.10.

73. **Task - Rename auto-build-claw to autobuild** (v1.0.5): Renamed plugin, module, and all artefacts from auto-build-claw to autobuild across the entire codebase<br>
    **Result**: Three directory renames via `git mv`: `auto-build-claw/` -> `autobuild/`, inner skill `auto-build-claw/skills/auto-build-claw/` -> `autobuild/skills/autobuild/`, and Python module `stellars_claude_code_plugins/engine/` -> `stellars_claude_code_plugins/autobuild/`. Updated 27 files (JOURNAL.md excluded as historical record) covering 6 name variants: `auto-build-claw` (kebab), `Auto Build Claw` (title), `.auto-build-claw` (artifacts dir), `auto_build_claw_resources` (pytest fixture -> `autobuild_resources`), `TestLoadAutoBuildClaw` (class -> `TestLoadAutobuild`), `claw commands` (comment). Engine code changes: `model.py` docstring and default artifacts_dir, `app.yaml` name/cmd/artifacts_dir/description (4 lines), `orchestrator.py` comment and 2 hardcoded bootstrap paths. All `stellars_claude_code_plugins.engine.*` imports updated to `stellars_claude_code_plugins.autobuild.*` across `__init__.py`, `orchestrator.py`, `pyproject.toml` entry point and package-data, 3 test files. Config updates: `marketplace.json` name and source, `.gitignore`, CI link checker path, increment-plugin-version command. Plugin-internal: 10 files under `autobuild/` (plugin.json, README, agents, commands, 3 skills, 4 docs). Repo docs: README (6 refs), fact_repository.md, devils_advocate.md, devils_advocate_program.md, readme-rewrite-analysis.md, 2 Medium articles. Final sweep caught 3 `Auto-build-claw` capitalization variants missed by first pass. Zero "claw" references remaining in tracked files. 287 tests passing.

74. **Task - SVG tools to pip package** (v1.0.5): Moved 5 SVG validation tools into stellars_claude_code_plugins module with unified CLI and defect-detection tests<br>
    **Result**: Moved 5 Python tools (115KB) from `svg-infographics/tools/` to `stellars_claude_code_plugins/svg_tools/` via `git mv` so they ship with `pip install`. Created `__init__.py` and symlinks back to `svg-infographics/tools/` to preserve the `<plugin>/tools` convention for SKILL.md references. Created unified CLI entry point `svg-infographics` (`cli.py`) with 5 subcommands: `overlaps` (check_overlaps), `contrast` (check_contrast), `alignment` (check_alignment), `connectors` (check_connectors), `connector` (calc_connector). Registered as `[project.scripts]` in pyproject.toml. Replaced all `sys.path.insert(0, TOOLS_DIR)` hacks in `test_svg_tools.py` with direct `from stellars_claude_code_plugins.svg_tools.X import Y` imports. Added `TestDefectDetection` class (6 tests): overlap detection by moving two text elements to same position, contrast failure by setting fill to `#eeeeee` on white, alignment violation by shifting element +3px off 5px grid, zero-length connector detection, CLI end-to-end contrast defect test. Added `TestSvgInfographicsCLI` class (8 tests): help output, no-args help, unknown subcommand error, and one test per subcommand (overlaps, contrast, alignment, connectors, connector) plus subcommand --help. Total tests: 301 (287 + 6 defect + 8 CLI).

75. **Task - SVG skill tool references** (v1.0.5): Updated all svg-infographics skills and commands to use pip-installed CLI<br>
    **Result**: Replaced all `python "$TOOL_DIR/check_*.py"` and `TOOL_DIR=<plugin>/tools` patterns with `svg-infographics <subcommand>` CLI syntax across 9 files: `validation/SKILL.md` (full tool docs rewritten with install instructions and new command syntax), `workflow/SKILL.md` (phases 3/5/6), `workflow/WORKFLOW.md` (phase 6 validation + checklist item 15), `svg-standards/SKILL.md` (verify rule, connector rule, troubleshooting section), and 4 commands (`validate.md`, `fix-layout.md`, `fix-style.md`, `create.md`). Also updated `svg-infographics/README.md` validation tools section with CLI examples. Zero old-style tool path references remaining in any svg-infographics markdown file.

76. **Task - Primitives and CSS checker** (v1.0.5): Added calc_primitives geometry generator and check_css validator to svg-infographics CLI<br>
    **Result**: Created `calc_primitives.py` - a geometry calculator that returns exact coordinates and anchor points for Claude agents to build SVGs without approximating positions. 14 primitive types across three categories: 2D (rect, square, circle, ellipse, diamond, hexagon, star, arc), 3D isometric (cube, cuboid, cylinder, sphere, plane), and curves (spline via pure Python PCHIP interpolation preserving monotonicity). Also generates coordinate axes (x, y, xy, xyz) with configurable ticks and labels. Each primitive returns named anchor points (center, top-left, vertices, tips, etc.) and an SVG template snippet. Added `shapely` to dependencies for precise geometric computations (area, centroid, bounding box) used in hexagon and polygon calculations. Created `check_css.py` - CSS compliance validator that checks: inline fill on text (must use CSS classes), forbidden colours (#000000/#ffffff), text opacity (reduces contrast), inline style attributes with colours, missing dark mode overrides for light-mode classes, and non-standard fonts. CLI updated to 7 subcommands. Added symlinks for both new tools in plugin `tools/` directory. 28 new tests: `TestCalcPrimitives` (20 tests covering all primitives including PCHIP monotonicity preservation and control point interpolation accuracy), `TestCheckCSS` (8 tests covering clean pass, inline fill detection, forbidden colours, text opacity, missing dark mode, CLI end-to-end, and real example smoke tests). 339 tests total.

77. **Task - SVG tool surface expansion** (v1.0.6, plugins v1.2.2): Added object-vs-background contrast, four-mode connectors, and a Fusion-360-style geometry calculator<br>
    **Result**: Three additions to the svg-infographics tool surface, all tested and wired through the unified CLI. **Object contrast** in `check_contrast.py` flags cards/shapes that blend into the document background in light or dark mode using WCAG SC 1.4.11 (3:1 max of fill or stroke), with a paired-shape merger so a strong stroke can rescue a faint fill, a proper SVG path bbox parser handling H/V commands, and a side fix where the path parser now reads `fill-opacity` (dropping example #01 text fails from 47 to 17). **Four connector modes** in `calc_connector.py`: `straight` (existing), `l` (axis-aligned right angle), `l-chamfer` (4px diagonal corner cut), `spline` (PCHIP through 3+ waypoints, parametrised by chord length so curves can loop). All modes share a unified result with `samples`, `path_d`, `trimmed_path_d` (arrowhead clearance baked in), and per-end `tangent`/`angle_deg`/arrowhead polygon. **New `calc_geometry.py`** tool exposed as `svg-infographics geom`, pure Python, eighteen subcommands across constraints (midpoint, extend, perpendicular foot, parallel/perpendicular construction, bisector), tangents (point-to-circle, two-circle external/internal), intersections (line/line, line/circle, circle/circle), polar layout (polar, evenly-spaced, concentric), attachment points (`attach --shape rect|circle` for snap-to-edge), and parallel offsets (line, polyline with mitered corners, rect inflate/deflate, circle, polygon, label standoff). Every command prints both the computed values and a paste-ready SVG verification snippet. **CLI help** rewritten so every subcommand has a one-sentence "what + when to use it" hint and the top-level `geom --help` shows a "When to reach for which subcommand" decision table. `cli.py` `SUBCOMMANDS` reorganised into VALIDATORS/CALCULATORS groups. **Skill updates**: added a "Tool Inventory" section to `WORKFLOW.md` and an "Available Tools" table to `workflow/SKILL.md` listing all calculators and validators with one-line capability hints, framed as consideration ("scan this list and ask 'is there a tool for this?'") not enforcement. Reinforced the PCHIP-not-bezier rule in `svg-standards/SKILL.md` and `commands/create.md` after observing a hand-written cubic bezier slip past the workflow. Total: 8 tools (`overlaps`, `contrast`, `alignment`, `connectors`, `css`, `connector`, `primitives`, `geom`) and 399 passing tests (60 new this session: 6 object contrast, 14 connector modes, 40 geometry).

78. **Task - Journal detail discipline rule** (plugins v1.2.2): Codified normal-vs-extended journal entry shape after rewriting #77 from a release log into a recap<br>
    **Result**: After writing entry #77 as a 600-word file-listing release log, user asked for "less details, just make a nice summary like you gave me before". Added a "Detail discipline" paragraph to `journal/skills/journal/SKILL.md` clarifying that exhaustive file paths, helper names, and per-class test breakdowns belong ONLY in **extended** entries; **normal** entries should read like a colleague's coffee recap (what was added, the headline numbers, and the why); **short** entries get one fact and one outcome. Saved the same guidance as a feedback memory (`feedback_journal_detail_level.md`) and indexed it in `MEMORY.md` so future sessions apply it without prompting. The end-of-turn summaries given in chat are the right shape for normal-level entries.

79. **Task - text-to-path tool + CI import guard** (v1.0.6, plugins v1.2.4): Added on-request text-to-path SVG tool and a regression test catching stale module references in CI workflow snippets<br>
    **Result**: Two related strands in one session. First, fixed a latent CI failure: `.github/workflows/ci.yml` still imported `stellars_claude_code_plugins.engine.*` after the rename to `autobuild` in entry #73, so the `test` and `test-install` jobs would have failed on next CI run. Repointed both jobs at `autobuild.model`, `autobuild.orchestrator`, and `autobuild.fsm`, then added `tests/test_ci_imports.py` which parses the workflow YAML, extracts every `from stellars_claude_code_plugins... import ...` statement out of `run:` blocks via regex, and importorlibs each in-process - any future rename now fails `make test` instead of waiting for CI. Six parametrized cases generated from the current workflow. Second, built the `text-to-path` tool the user requested for embedding text as glyph outlines (custom fonts, print portability, deterministic bounding boxes for fit-to-width). New module `stellars_claude_code_plugins/svg_tools/text_to_path.py` uses `fontTools.pens.svgPathPen.SVGPathPen` + `TransformPen` to draw every glyph into a single shared pen with the running cursor baked in as a translation, producing one merged `<path d>` wrapped in a parent `transform="translate scale"` that handles font-units to user-units and the font Y-up to SVG Y-down flip. API mirrors `<text>` semantics: `(x, y)` is the baseline, `anchor` is start/middle/end, `fill` and `css_class` are mutually optional so theme-controlled paths stay possible. `fit_width` scales the path uniformly down (never up) when natural advance exceeds the cap, preserving aspect - no `textLength` glyph stretching. CLI exposed via the unified `svg-infographics text-to-path` subcommand with both human and `--json` output. `fonttools` added as a `[fonts]` optional extra so the base install stays lean - the SKILL.md documentation explicitly marks the tool as "ON REQUEST ONLY" with the four legitimate triggers (custom font embedding, print/handoff portability, deterministic headline bbox, branding marks) and the tradeoffs (non-editable text, ~5-20x larger files, explicit font path required - no system font resolution). Symlinked back into `svg-infographics/tools/` to keep the plugin convention. 16 new `TestTextToPath` tests covering basic render, baseline semantics, all three anchor modes, fit-width scale-down + no-op-when-fits, fill/class/inherit, validation errors, Unicode `.notdef` fallback, SVG round-trip parse, and three CLI smoke tests; plus one new help-listing assertion in `TestSvgInfographicsCLI`. Total: 422 passing tests (399 prior + 6 CI guard + 16 text-to-path + 1 CLI listing). Eight tools now shipped in the `svg-infographics` CLI: seven core (overlaps, contrast, alignment, connectors, css, primitives, geom, connector) plus the on-request text-to-path.

80. **Task - README marketplace banners** (plugins v1.2.5): Created seven stellars-tech themed SVG infographics for the README - one marketplace hero plus one banner per plugin<br>
    **Result**: Built 7 production SVGs in `assets/svg/` following the svg-infographics 6-phase workflow, all using the existing `theme_swatch_0_stellars-tech.svg` palette (teal primary `#005f7a`/dark `#5cc8e0`, orange-brown accent `#7a4a15`/gold `#d4a04a`, Segoe UI, flat-top rounded-bottom cards with height-5 accent bars at opacity 0.6 and 0.04 fill). The marketplace hero `01_marketplace_overview.svg` (900x460) lays out all six plugins in a 3-column x 2-row grid grouped by category (Build & Iterate, Critique & Validate, Communicate & Track) with a title band, version/test stats on the right, and a bottom install-line strip. The six plugin banners (900x180-200) share a left-title-zone + right-visual-zone template held together by a vertical divider and a consistent chip-row pattern: `02_autobuild_phases.svg` shows the eight-phase FSM lifecycle as numbered cards with a build-core highlight in gold; `03_devils_advocate_scoring.svg` has a 5x5 Fibonacci risk matrix (likelihood x impact) with a safe-opacity scheme to keep 7px cell labels above WCAG AA - only opacities `<=0.18` for dark text and `>=0.85` for white text, no "dead zone" - plus three sample concerns with iteration arrows; `04_svg_infographics_workflow.svg` shows six numbered phase circles with chevron progressions and eight tool badges grouped into validators (teal) and calculators (gold); `05_datascience_pipeline.svg` shows the project scaffold dirs above a seven-cell notebook section pipeline (header, GPU, imports, config, data, model, eval); `06_document_processing_grounding.svg` is a three-card flow (sources -> grounding -> compliant output) with a "[3 SOURCES CITED]" badge replacing inline citation circles; `07_journal_audit.svg` shows the archive bucket on the left and a fade-in timeline of entries 69..78 with the most recent at full opacity. Several iteration cycles were needed to satisfy the validators: arrow lines between cards consistently triggered `track-line vs card` outer-padding overlaps so they were converted to polygon-only chevrons; the SOURCES card overlapped the divider in `06` so the divider was dropped; the devils-advocate matrix originally used both teal and gold cells but the gold backgrounds caused 9 AA contrast failures at 7px text, fixed by switching to teal-only with a "safe opacity bands" scheme. Validation final state: 7/7 SVGs have zero overlap violations and zero text contrast failures (only the marketplace hero retains 3 classified-accepted 0.4% card-vs-strip outer-padding artifacts that match the reference theme_swatch behaviour); object contrast warnings are universal across all SVGs because the checker doesn't resolve CSS-class strokes for SC 1.4.11, same as every reference example. Verified via headless-chromium rendering of light + dark gallery pages (980x2000) - the `@media (prefers-color-scheme: dark)` block in each SVG correctly swaps teal to cyan and brown to gold, all type stays readable, family looks consistent. README updated with `<img>` tags inserted after the badges block (hero) and at the start of each plugin section. Required `sudo apt-get install libnss3 libnspr4 libatk1.0-0t64 libatk-bridge2.0-0t64 libgbm1 libxkbcommon0 libxcomposite1 libxdamage1 libxfixes3 libxrandr2 libpango-1.0-0 libcairo2 libasound2t64` to get the playwright-bundled chromium-headless-shell to load. Plugin marketplace bumped 1.2.4 -> 1.2.5 across all six `plugin.json` files and `marketplace.json` metadata.

81. **Task - Hero cleanup + icon placement + README arrows** (plugins v1.2.6): Dropped stale stats from the hero SVG, codified upper-right as the default icon anchor, and swapped README phase arrows to unicode<br>
    **Result**: Three small polish items. The marketplace hero `01_marketplace_overview.svg` was carrying two right-aligned header lines (`v1.2.5`, `422 tests passing`) that aged poorly the moment the next version bump landed - stripped both `<text>` elements from the header band so the hero now shows only the title, tagline, and capability strip. Added a "Default Placement" subsection under `svg-standards/SKILL.md` "Icon Sourcing Policy" encoding the convention that icons anchor to the upper-right of the composition (graphic-level icons inside the header band at `viewBox.width - margin - icon_size`, card-level icons at `card.x + card.w - 6 - icon_size, card.y + 6`), with rationale (western reading path terminal scan) and explicit override cases (symmetric grids, process flow row anchors, timeline event markers). Finally replaced ASCII `->` with unicode `→` in the autobuild workflow table in README.md lines 52-56 per explicit request, overriding the default typography rule for this specific table only.

82. **Task - Connector rewrite, Bezier manifold, empty-space tool, callout workflow** (v1.0.11, plugins v1.3.0): Major svg-infographics refactor - canonical manifold with Bezier tangent control, auto-edge routing, curve/shape midpoint helpers, empty-space detection, pygal charts with dark-mode CSS injection, article_02 draft, PyPI release<br>
    **Result**: Rewrote `calc_connector.py` after user complained strands had vertical stubs at merge/fork points and that connectors routed centre-to-centre instead of edge-to-edge. Dropped the global `first_axis` horizontal-first rule from svg-standards. Rebuilt the `manifold` mode around a canonical topology: single merge point (`spine_start`), single fork point (`spine_end`), N start strands converging at merge, M end strands diverging at fork, a spine connecting the two. Went through three geometry iterations - perpendicular-line merge points, wire-harness bundles (rejected), finally cubic Bezier via the `bezier` Python library (`bezier.Curve.from_nodes`, `evaluate_multi`) with tangent magnitude driven by a `tension` parameter mapped `tangent_mult = 0.9 - 0.7 * t` so 0=floppy, 1=stiff. Each point can optionally carry a direction as a 3-tuple with compass string (`N`/`NNE`/`NE`/.../`NW`) or numeric degrees (0=N, 90=E), implemented via `_direction_to_unit_vector` and `_unpack_point_with_direction`. Strands without explicit direction inherit the spine direction at merge/fork. Added auto-edge routing: `calc_connector` accepts `src_rect`/`tgt_rect` or `src_polygon`/`tgt_polygon`, runs a centroid-to-centroid ray, finds the perimeter intersection via `_rect_edge_intersection` or `_polygon_edge_intersection` (CCW segment-segment test), snaps endpoints to the edge midpoint. Added per-end `standoff` (scalar or 2-tuple). `calc_spline` Bezier branch: when `start_dir`/`end_dir` are provided it returns cubic Bezier instead of PCHIP since PCHIP doesn't honour tangents. `calc_geometry.py` gained `curve_midpoint(points)` (arc-length midpoint + tangent + total length on any polyline) and `shape_midpoint(points)` (area-weighted centroid of closed polygon) with CLI handlers `curve-midpoint` and `shape-midpoint`. New tool `calc_empty_space.py`: recursive quadtree free-region detection - coarse NxN grid scans occupied shapes (rects or polygons), occupied cells subdivide 3x3 up to `max_depth`, free cells unioned via `shapely.ops.unary_union`, boundaries returned as polygons sorted by area desc with Douglas-Peucker simplification. Used for callout placement - find empty islands, fit text bboxes, audit leader crossings. Rewrote `charts.py` around pygal, removed the hardcoded THEMES dict - caller provides `colors`, `fg_light`, `fg_dark`, `grid_light`, `grid_dark`, and `_inject_dark_mode_css` post-processes pygal output with a `@media (prefers-color-scheme: dark)` block. Added `check_collisions.py` (pairwise shapely LineString intersection, classifies crossing/near-miss/touching). `cli.py` registered three new subcommands: `empty-space`, `charts`, `collide`. Core deps moved from optional to required: `bezier`, `fonttools`, `numpy`, `pygal`, `pyyaml`, `scipy`, `shapely`, `tiktoken`, `transitions`. Skill docs rewritten in telegraph style (no articles/copulas) to save tokens: `svg-standards/SKILL.md` gained "Edge midpoint rule", "Canonical manifold" with tension semantics, "Auto-edge mode", "Callout placement rules" (5 numbered rules), "Callout construction workflow" (5 steps using empty-space tool: find islands → compute text bbox → fit box inside island → audit leader crossings → render text). `workflow/SKILL.md` tool inventory updated with `empty-space` and `collide`. Docstrings across `calc_connector.py` gained "REMINDER: endpoints must be on shape EDGES, not centres". Wrote `docs/medium/article_02_svg-infographics.md` (~2200 words, 9 sections) using the article-writer + medium-article skills, pulling images from a new `images_article_02/` directory featuring `06-manifold-showcase.svg` (5 sources → 3 sinks canonical manifold + direct auto-edge connector + L-chamfer + 8 callouts + legend). The showcase went through many iterations to resolve overlaps between strands and callout text. Tests grew to 479 passing: 10+ new manifold canonical-model tests, Bezier spline with directions, `curve_midpoint`, `shape_midpoint`, 10 empty-space tests (basic / no_shapes / sorted_by_area / fully_occupied / polygon_shape / recursive_subdivision / simplify / boundary_within_canvas / CLI / CLI_json), auto-edge mode with rects and polygons, updated legacy straight-mode tests for the new polyline-shaped result (`total_length`, `end["tip"]`). Also renamed `docs/medium/images/` → `docs/medium/images_article_01/` and cleaned up stale Claude project sessions. Published `stellars-claude-code-plugins` v1.0.11 to PyPI, bumped plugins to v1.3.0.

83. **Task - Callout cross-collision checker + geom contains + rect-edge + callout workflow hardening**: Extended overlap checker with callout-group awareness, added two new `geom` subcommands, tightened empty-space defaults, and rebuilt 06-manifold-showcase callouts via the full tool chain<br>
    **Result**: User caught that my callout placement workflow was bbox-based rather than polygon-based — "auto-edge routing" text sat inside Shape B because I treated the island's axis-aligned bounding rect as the fit region when islands can be L-shaped or horseshoe. Fixed by adding `geometry_in_polygon(inner, polygon)` to `calc_geometry.py`, returning two flags per check: `contained` (all inner points covered by outer via `shapely.covers`) and `convex_safe` (convex hull of inner also covered - catches the case where two inner points are inside a concave container but the line between them exits and re-enters through a notch). Inner geometry accepts any of 5 kinds: `point`, `bbox`, `line`, `polyline`, `polygon`. When the strict check fails the function enumerates pairwise point segments and returns `exit_segments` for debugging. Exposed as `svg-infographics geom contains --polygon <outer> [--point | --bbox | --line | --polyline | --inner-polygon]` with 9 new tests covering square (convex), U-shape (concave) pass/fail cases, and CLI smoke. Added a second new subcommand `svg-infographics geom rect-edge --rect x,y,w,h --from px,py` that clips the ray from rect centre toward an external point at the rect perimeter, implemented via `rect_ray_exit()`. Used for leader anchor computation: inflate text bbox by standoff (`geom offset-rect`), then `rect-edge --rect <inflated> --from <target>` returns the point where the leader meets the bbox edge — replacing the "glue leader to bbox edge" anti-pattern with a 3px visual gap. 5 new tests for right/bottom/diagonal sides and degenerate input. Extended `stellars_claude_code_plugins/svg_tools/check_overlaps.py` with a new `parse_callouts()` + `check_callouts()` pair that walks `<g id="callout-*">` groups and extracts text bboxes (font-size pulled from `.callout-text` CSS) + leader segments (supports `<line>`, `<polyline>`, `<path>` via a mini SVG path parser handling M/L/H/V/Z). Pairwise shapely check reports three classes of violation: `leader of X crosses text of Y`, `leader of X crosses leader of Y`, `text of X overlaps text of Y`. Wired into the `check_overlaps` CLI as a new `CALLOUT CROSS-COLLISIONS` output section folded into the SUMMARY line. 8 new `TestCalloutCollision` tests. Added `tolerance` (default 20, callout minimum) and `min_area` (default 500 px², drop slivers too small to fit a callout text bbox) parameters to `calc_empty_space.find_empty_regions()` — when tolerance>0, each free region is shrunk inward via `shapely.buffer(-tolerance, join_style=2)`; when min_area>0, regions below the threshold are dropped before sorting. CLI gained matching `--tolerance` and `--min-area` flags with a stderr warning when `--tolerance` is below 20. Kept `max_depth` default at 3 after user reversed an earlier depth-2 tweak. Updated existing empty-space tests to pass `tolerance=0` explicitly (preserving old semantics) and added 6 new tests covering the tolerance shrink, thin-region drop, min-area filter, and default sentinels. Skill docs hardened: `svg-standards/SKILL.md` callout construction workflow expanded from 5 to 7 steps with explicit pre-audit and post-audit `overlaps` gates; step 4 now mandates `geom contains --polygon <island> --bbox <text-bbox>` with the `contained=YES convex-safe=YES` pass condition, plus the warning that ISLANDS CAN BE L-SHAPED; rule 6 added to placement rules requiring leader anchor at `standoff` px short of text bbox via `offset-rect` + `rect-edge`; empty-space documented as "not callout-only" (legends, badges, logos, icons, decorative imagery, slot validation). `workflow/SKILL.md` tool inventory tightened: the `empty-space` entry now describes input/output and use cases instead of the quadtree algorithm (per feedback: "no need to explain algorithm, more important how to use and what returns"), the `geom` entry picked up `contains` and `rect-edge`, the `overlaps` entry mentions the new cross-collision check. The message from the user throughout this session: CLI help and skill text must be conservative (telegram-ish style, maximum information per token, no fluff) because those strings flow back into Claude's context on every workflow run; docstrings inside modules can stay verbose since they're only read on deep debugging. Applied the new 7-step workflow to `06-manifold-showcase.svg`: shifted all point-to-point shapes down 20px to clear the section header, replaced 4 existing callouts with 5 new ones (merge, fork, standoff, auto, chamfer) computed via `find_empty_regions` → `geometry_in_polygon` → `rect_ray_exit` pipeline so the text bboxes land inside the returned polygon and leaders anchor 3px short of each bbox. Post-audit reports 0 callout cross-collisions. Session tests: 517 passing (+17 from the 500 baseline).

84. **Task - SVG-native find_empty_regions: bitmap rewrite, svgelements adapter, Pillow text metrics** (plugins v1.3.1): Replaced the quadtree+shapely empty-space detector with a bitmap + scipy distance transform pipeline that parses real SVG files end-to-end<br>
    **Result**: User caught two progressively deeper problems in the empty-space detector. First: the shapely `unary_union` of quadtree cells was leaving zero-width hairline seams at coarse-cell boundaries, and the subsequent `buffer(-20)` was inflating those seams into visible notches in the upper-left island at x=105..145 and into "zero-width left artefacts" along x=55.56 / x=57.87. Debugged by dumping the pre-buffer polygon exterior and finding long chains of duplicate vertices at identical x coordinates - signatures of the seam pathology. Second: even after a one-line `buffer(0)` heal, the callers were hand-building shape lists that were either too loose (passing strand bundles as bboxes, over-occupying 90+ K pixels of shoulder space that callouts could legitimately use) or too drift-prone (requiring the caller to keep the shape list in sync with the SVG being designed). Addressed both by **rewriting the core algorithm** (bitmap + Euclidean distance transform + scipy.ndimage.label + pure-numpy Moore trace) and **flipping the public API** to accept an SVG source directly (Path / str / bytes / stream). The new `find_empty_regions(svg, tolerance, min_area, canvas, exclude_ids)` parses the SVG via `svgelements.SVG.parse`, walks the element tree recursively with ancestor-id pruning for `exclude_ids=("callout-*",)` (so placed callouts don't pollute the obstacle set on re-runs), converts every drawable element into one of three temporary surrogate primitives (rect, polyline with stroke, polygon) via the element-to-surrogate converter, and rasterises them into a 1 px occupancy grid. Beziers and arcs are adaptively sampled via `seg.length()` at `max(10, min(100, round(arc_length)))` points per segment; circles/ellipses become 32-sided polygons; SVG `<text>` elements become bboxes with REAL font metrics via `PIL.ImageFont.load_default(size=N).getlength(text)` (Pillow 12 bundles a scalable TrueType so no external font file is needed, and the metrics are cached per size). Text-anchor `start`/`middle`/`end` is now honoured correctly - the previous `len * font_size * 0.55` heuristic was both inaccurate (13% overshoot at 15 px) and completely wrong for middle/end anchored labels which had their bboxes offset by up to a full text width. The rasteriser uses three code paths: `_raster_rect` (numpy slice), `_raster_polyline` (per-segment point-to-segment distance on a local AABB, threshold by `stroke/2`), `_raster_polygon` (scanline crossing-count per row for even-odd fill). Canvas edges count as implicit obstacles via a 1-pixel False padding around the free mask before `distance_transform_edt`. Added `svgelements>=1.9` and `pillow` as core deps - both pure Python, zero C extensions, total ~5 MB install footprint. Performance on `06-manifold-showcase.svg`: 645 ms quadtree → 91 ms SVG-native bitmap (7x speedup) while detecting 18-19 islands versus the old 7+1 fragmented result. Crucially the new algorithm now correctly surfaces the previously-hidden "shoulder" regions between src cards (areas ~979-1092 px² at x=238..300 between each pair of consecutive src_n / src_n+1 cards where the strands fan out) - these were invisible under the loose strand-bundle bbox that the old hand-built shape list was passing. A full rewrite to the pure-Python Moore boundary tracer with collinear-vertex collapse replaces the shapely polygon export for per-island boundaries, avoiding the preserve_topology=True edge cases that would otherwise keep hairline spikes alive. Added `exclude_ids` kwarg with default `("callout-*",)` glob pattern via `fnmatch` so callers can append new callout groups to the SVG and re-run without the placed groups interfering. The svgelements walk is RECURSIVE (not `svg.elements()` flat iteration) precisely because exclusion needs to prune the whole subtree of a matching `<g id="callout-*">` group - the flat iteration would exclude only the Group wrapper itself and still rasterise its children. Migrated all 21 empty-space tests from shape-list literals to inline SVG XML strings (test helpers build minimal `<svg viewBox="..."><rect .../></svg>` documents), added 4 new coverage tests (polygon element via `<polygon>`, CSS stroke cascade via `<style>.big { stroke-width: 10 }`, exclude_ids pruning, Path / bytes / bad input), and two targeted text-metric tests (middle-anchor placement, text-length-affects-free-area). Updated `svg-standards/SKILL.md` callout construction workflow to call `svg-infographics empty-space --svg file.svg` instead of building a shape list; added a "Callout naming convention" section documenting the mandatory `callout-*` id prefix plus `.callout-text` / `.callout-line` CSS classes so that `check_overlaps` cross-collision detection and `find_empty_regions` exclusion can both find them. Updated `workflow/SKILL.md` tool inventory entry for the SVG-native API. Regenerated `06-manifold-showcase-debug.svg` to show 18 red empty-region polygons including the four shoulder regions between src cards that were previously hidden. Files changed: `pyproject.toml` (+svgelements, +pillow), full rewrite of `stellars_claude_code_plugins/svg_tools/calc_empty_space.py` (~650 lines from ~200), `tests/test_svg_tools.py` (+2 new tests, migrated 21 existing to SVG input), `svg-infographics/skills/svg-standards/SKILL.md` (naming convention + workflow update), `svg-infographics/skills/workflow/SKILL.md` (tool inventory), `docs/medium/images_article_02/06-manifold-showcase-debug.svg` + `.png` (regenerated). Session tests: 524 passing (+7 from the 517 baseline: 4 SVG input variants, 2 text metric tests, 1 polygon element conversion). Late-session fix: user caught that legend-row text wasn't participating in empty-space detection - seven phantom free regions appearing at y=643..659 in the legend zone where the "Legend" / "Spine (stiff trunk)" / "Merge / fork point" / "Manifold strand" / "Direct straight" / "L-chamfer" / "Callout leader" labels should have been obstacles. Root cause: svgelements REIFIES transforms into coordinates for Rect / Circle / Ellipse / Line / Polyline / Polygon / Path when the transform is pure translate+scale, but leaves `element.transform` non-identity on Text, Image, and any rotated shape. My converter was reading `elem.x` / `elem.y` directly on Text elements, which are LOCAL coordinates relative to the parent group - so a legend text at `<text x="240" y="0">` inside `<g id="legend" transform="translate(40, 655)">` was being rasterised at world (240, 0) instead of world (280, 655), falling off the top of the canvas and leaving the legend row unblocked. Fixed by applying `elem.transform` explicitly to every coordinate of every element kind (Rect, Circle, Ellipse, Line, Polyline, Polygon, Path, Text, Image), so the surrogate SVG always carries final world-space geometry regardless of whether svgelements happened to reify the transform. Added `_tx_point(transform, x, y)` helper and `_tx_is_axis_aligned(transform)` predicate. Rotated / skewed rects now emit as 4-corner polygons instead of axis-aligned rect primitives. 06-showcase island count dropped from 18 to 11 - every remaining region is a legitimate free zone (2 above/below spine, 3 lower point-to-point, 2 sink gap strips, 4 shoulder regions between src cards). Upstream: text-anchor `start`/`middle`/`end` is honoured AFTER applying the transform to the anchor point, so middle-anchored titles at large font sizes now produce correctly-centred bboxes even when their parent group has a translate. The lesson: never trust an SVG parser to have already reified transforms for you - always apply `element.transform` explicitly and let the identity matrix be a no-op for the elements that were already reified.

85. **Task - Plugin README realignment, svg-infographics capability rewrite, skill cleanup, journal example, debug overlay annotation** (v1.0.13 PyPI, plugins v1.3.2): Six plugin READMEs realigned to capability-and-usage scope, svg-infographics README rewritten around five named capabilities with use cases, svg-standards skill tightened to telegram style with a mandatory five-layer z-order, `[fonts]` optional-extra drift removed across three files, 06-manifold-showcase debug overlay annotated with empty-region labels for an article, three stale repo-root artefacts deleted<br>
    **Result**: Session opened with the user pointing out that plugin READMEs had drifted into deep workflow documentation - the svg-infographics README in particular had a dedicated Callout Naming Convention section, a 7-step empty-space workflow walk-through, and multi-paragraph design-principle bullet lists, all content that belongs in SKILL files not in a capability overview. Ran six parallel agents (Task tool, `general-purpose` subagent) to realign each plugin README to a strict "what + where" scope: each agent read its plugin, identified deep how-to content, trimmed it to a one-line mention with a pointer to the relevant SKILL.md, and kept only title + description + install + commands table + skills table + one usage example + documentation pointers. Line counts dropped: autobuild 97 -> 68, devils-advocate 101 -> 74, datascience 147 -> 76, journal 74 -> 58, document-processing untracked -> 58, svg-infographics 163 -> 97. The svg-infographics README was then REWRITTEN a second time around five foundational-first capability groups after the user asked for capability-centric framing: (1) Design Foundations covering grid-first layout, shape primitives, theme/CSS/dark mode, typography, 6-phase mandatory workflow, and the Fusion-360-style geometry sketch-constraint toolkit; (2) Connectors covering five routing modes (straight / l / l-chamfer / spline / manifold) with canonical Sankey manifold and auto-edge routing; (3) Callouts and Empty Space with the `callout-*` naming convention (group id prefix, `.callout-text`, `.callout-line`) folded inline as prose rather than a dedicated section, SVG-native free-region detection, and the placement workflow pointer; (4) Charts via pygal with caller-provided palette and injected `@media (prefers-color-scheme: dark)`; (5) Validation covering five checkers plus pairwise connector collision as the mandatory pre-delivery gate. Added five use cases (branded banner, annotate dense diagram, Sankey flow, port foreign SVG to theme, place legend on populated canvas) showing how capabilities compose. Kept Commands, Skills, and Tool Inventory as reference tables at the bottom for agents that scan by grep. Separately rewrote the `svg-standards/SKILL.md` Z-Order Layering section to mandate five named top-level groups (`<g id="background">`, `nodes`, `connectors`, `content`, `callouts`) with explicit rules that every drawable element belongs to exactly one layer and callouts always live on their own topmost layer; rewrote the Arrow Construction and Callout Construction Workflow sections in telegram style dropping articles and copulas while preserving the 7-step workflow with its pre/post audit gates; expanded the Quick Reference from 11 to 14 entries adding the layer convention and callout/connector workflow reminders. Removed the `[fonts]` optional-dependency drift: `fonttools` was already a core `pyproject.toml` dependency so the `pip install 'stellars-claude-code-plugins[fonts]'` guidance in the README install block, the `skills/validation/SKILL.md` "Eight tools" header and dedicated install block, and the `stellars_claude_code_plugins/svg_tools/text_to_path.py` module docstring plus `_require_fonttools` error message plus CLI error message was wrong - most users reach the package via Claude Code and expect every tool to work without extras. Rewrote validation SKILL.md to say "Twelve tools shipped ... no optional extras required", updated the text_to_path module docstring and both error messages to say "core dependency - reinstall the package", updated the corresponding pytest skip-reason in `tests/test_svg_tools.py`, verified `pyproject.toml` has no `[fonts]` extra to remove (it was always only in docs). Annotated `docs/medium/images_article_02/06-manifold-showcase-debug.svg` for article use: moved the 5px-standoff callout to a diagonal-leader placement at baseline (710, 86) with leader (808, 116) -> (777, 99) at 35 px length and 61° from vertical (down from 90° / straight-up previously), added a `.empty-label` CSS class (fill `#e03020`, italic, bold, with dark-mode `#ff8060` override), added eleven empty-region labels each of whose text bbox was verified to be fully contained inside the matching polygon via `shapely.Polygon.covers(box)` and verified to not collide with any existing callout text bbox (merge, fork, auto, chamfer, standoff), used Pillow `ImageFont.load_default(size=N).getlength(text)` for accurate per-font metrics, extended the bottom-row legend with an "Empty region" entry using a single dashed `<line class="empty-region">` swatch matching the actual region styling. Also added a new "Documentation + cleanup entry" example to `journal/skills/journal/references/examples.md` showing the single-dense-paragraph shape for sessions that are all docs and plumbing cleanup with no new feature or test - uses this very session's work as the reference entry. Deleted three stale repo-root artefacts from a prior devils-advocate run (`devils_advocate.md`, `devils_advocate_program.md`, `fact_repository.md`) that had drifted out of scope. Linted with `uv run ruff format` + `ruff check --fix` which surfaced two remaining issues: a `lambda x, y: _tx_point(transform, x, y)` binding in `_element_to_surrogates` (fixed by rewriting as nested `def tx(x, y)`) and an unused `last_point = seg.end` assignment in `_sample_path_to_segments` (deleted four occurrences, no reads). Tests: 524 passing baseline maintained - pure docs, plumbing, and debug-overlay session with no new features or regressions. Plugin marketplace bumped v1.3.1 -> v1.3.2 across all six `plugin.json` files and every `marketplace.json` entry, PyPI `stellars-claude-code-plugins` published as v1.0.13.

86. **Task - Leaderless callout placement fix: standoff asymmetry + centre-distance scoring + target-coordinate correction** (v1.0.14 PyPI, plugins v1.3.3): Leaderless callouts in `docs/medium/images_article_02/06-manifold-showcase.svg` (5 INPUTS, 3 OUTPUTS, spine) were placed far from their intended visual anchors - the spine label floated in mid-strand whitespace, the input/output labels drifted off-centre above their card groups, and the user flagged the failure mode in three successive iterations<br>
    **Result**: Root cause was a two-part design bug in `stellars_claude_code_plugins/svg_tools/propose_callouts.py`. First, `leaderless_standoff` defaulted to 20 px matching the leader-callout standoff, which eroded 20 px around every obstacle and made the free mask so restrictive that the spine target at `(530, 215)` - only 15 px from the spine line at `y=230` - fell inside the dead zone, forcing the text bbox up to `y <= 210` and leaving a visually-obvious 22 px gap between the label and the line it was supposed to annotate. Fixed by dropping `leaderless_standoff` default from 20.0 to 5.0, since leaderless text IS the pointer and must sit close to its target (leader callouts need the 20 px because the leader inflates the text bbox by that amount for arrowhead clearance; leaderless has no such constraint). Added a matching `--leaderless-standoff` CLI flag with the same 5 px default for explicit overrides. Second, the leaderless scoring function measured distance from the TARGET to the nearest text BBOX EDGE with a sweet-spot of 8 px (the old `LEADERLESS_SWEET_SPOT` constant), which has a direction-agnostic level-set: any bbox corner 8 px from target scores identically, so horizontally-symmetric labels like "5 INPUTS" drifted to whichever grid point won a tiebreaker, landing off-centre above the card group instead of centred on the target. Replaced edge-distance with CENTRE-distance (`math.hypot(cx - target[0], cy - target[1]) ** 2 * 4`), which has a single unique minimum when the bbox centre equals the target point - the solver now converges to centred placements for symmetric labels. Deleted the `LEADERLESS_SWEET_SPOT = 8.0` constant, updated the `CalloutRequest` docstring and `propose_callouts` docstring to describe the asymmetric standoffs and centre-seeking scoring. Third, even with the scoring fix the 5 INPUTS / 3 OUTPUTS labels still looked wrong because the JSON fixture targets were stale: `(140, 40)` and `(890, 40)` sat in the title/subtitle band (title at `y=30`, subtitle at `y=48`) rather than above the card groups (src card tops at `y=70`, sink card tops at `y=90`). Updated `/tmp/callouts-06.json` targets to `(140, 62)` and `(890, 82)` so centring puts the labels immediately above each card top with a 5 px standoff clearance. Regenerated the SVG via the tool, landing `5 INPUTS` at baseline `(119, 60)` centred at `(138, 56.75)`, `3 OUTPUTS` at `(867, 80)` centred at `(889.5, 76.75)`, and `spine` at `(519, 220)` centred at `(531, 216.75)` with the text bottom 3 px above the spine line. Verified visually via cairosvg at 3000 px width and cropped top strip - all three labels now sit exactly where a human designer would place them. Tests: all 533 passing including the 9-test `TestProposeCallouts` suite, which was unaffected because the tests exercise placement-finding and CLI mechanics rather than specific scoring constants. Also folded in the previously-uncommitted propose_callouts feature work from the same session: the new `propose_callouts.py` module (~750 LOC, dataclass-based API with `CalloutRequest`/`CalloutProposal`, greedy solver with 5 random-ordering restarts, numpy mask-based obstacle rasterisation via `scipy.ndimage.distance_transform_edt`, STRtree-accelerated pairwise conflict detection, 935 ms convergence for 8 callouts on the showcase), its CLI registration as `svg-infographics callouts` in `stellars_claude_code_plugins/svg_tools/cli.py`, the `TestProposeCallouts` test class in `tests/test_svg_tools.py`, and skill-doc updates to `svg-infographics/skills/svg-standards/SKILL.md` + `svg-infographics/skills/workflow/SKILL.md` teaching the tool as the primary callout placement path with manual `empty-space` + `geom contains` + `geom rect-edge` kept as the debug fallback. Plugin marketplace bumped v1.3.2 -> v1.3.3 across all six `plugin.json` files and every `marketplace.json` entry, PyPI `stellars-claude-code-plugins` published as v1.0.14.

87. **Task - Telegraph-style SKILL.md rewrite across all six plugins, README callouts refresh, KOLOMOLO banner removal** (v1.0.15 PyPI, plugins v1.3.4): User asked for telegraph-style prose in every SKILL.md across the marketplace, an updated `svg-infographics/README.md` teaching the new leader / leaderless callout modes, and removal of the KOLOMOLO sponsorship banner from top-level README files<br>
    **Result**: Session started as a follow-up on the leaderless callout fix shipped in entry 86: the user wanted the README and skill docs to explicitly teach the `svg-infographics callouts` tool with the new leader / leaderless asymmetry, then wanted every SKILL.md file in every plugin rewritten in telegraph style to match the voice already used in `svg-infographics/skills/svg-standards/SKILL.md` (terse imperative and declarative fragments, no articles, no copulas, no "you should" filler), then asked for the `Brought To You By KOLOMOLO` shields.io badge removed from the root `README.md` and `devils-advocate/README.md` since the banner no longer reflects the project's current relationship. Updated `svg-infographics/README.md` capability #3 "Callouts and Empty Space" to lead with the `svg-infographics callouts` tool as the primary path, document both leader mode (20 px standoff, scored on leader length sweet spot 55 px + diagonal angle + target distance + preferred side) and leaderless mode (5 px standoff, centre-distance-to-target scoring so horizontally-symmetric labels settle centred), kept the `callout-*` namespace rule inline as prose, added the `callouts` subcommand to the tool inventory table, and rewrote the dense-diagram use case around the new three-step workflow (`overlaps` pre-audit, `callouts` propose, `overlaps` post-audit). Updated `svg-infographics/skills/svg-standards/SKILL.md` Callout construction workflow with a new "Leader vs leaderless" subsection listing use cases for each mode plus a picking rule, and added a common-failure-mode note about stale target coordinates (the spine/inputs/outputs debugging from entry 86 - user gives a target in the wrong visual region and the tool dutifully places text AT that wrong region). Updated `svg-infographics/skills/workflow/SKILL.md` callouts tool inventory entry with the correct `leaderless_standoff=5` default and centre-distance scoring note. Removed the KOLOMOLO shields.io badge line from `README.md` (line 7) and `devils-advocate/README.md` (line 6), leaving the GitHub Actions / PyPI version / downloads / Python / JupyterLab badges intact. Dispatched six parallel general-purpose subagents via the Task tool, one per plugin (autobuild, devils-advocate, datascience, journal, document-processing, svg-infographics), each instructed to read every SKILL.md in its plugin and telegraph the prose paragraphs while preserving YAML frontmatter, code blocks, command examples, section headers, bullet structures, technical facts, and all reasons-for-rules. Each agent used Edit tool with specific old_string/new_string replacements rather than full-file rewrites to keep code blocks pristine. Results: datascience (6 files, ~25% prose compression), document-processing (4 files, ~25-35% compression on validate-document/pdf-pro/process-documents, pdf mostly code blocks), autobuild (3 files, ~25-35% compression across benchmark-writer/program-writer/autobuild), devils-advocate (5 files, ~15-25% compression preserving all Fibonacci scoring and persona rules), svg-infographics (4 files, 15-25% compression - svg-standards agent skipped already-telegraphed sections like Arrow Construction and Callout workflow, edited only the remaining prose-heavy areas like CSS Theme Classes intro, Z-Order narrative, and Troubleshooting), journal (1 file, fragment-style declarations replacing "the X is Y" framings). Total files touched across the six-agent fan-out: 23 SKILL.md files across six plugins, plus the three root README / docs files and two skill docs updated in the foreground. Transformations dropped articles (a/an/the), copulas (is/are/was/were), and filler phrases ("you should", "in order to", "make sure to", "remember that") while preserving every command example, table, bullet structure, number, file path, function name, and rule rationale. Tests: all 533 passing - docs-only session with no code changes outside the already-shipped `propose_callouts.py` work. Plugin marketplace bumped v1.3.3 -> v1.3.4 across all six `plugin.json` files and every `marketplace.json` entry, PyPI `stellars-claude-code-plugins` published as v1.0.15.

88. **Task - Charts generator dark-mode palette + 7:1 contrast audit**: `svg-infographics charts` tool extended with `--colors-dark` and a WCAG contrast audit so chart SVGs render readably in both light and dark contexts<br>
    **Result**: `_audit_palette` enforces 7:1 min / 10:1 ideal contrast for every series against its background (`#ffffff` light, `#0b0b0b` dark). CLI prints both palettes and backgrounds to stderr on every invocation and emits `too pale - use a darker shade` / `too dark - use a brighter shade` hints with exact ratios for any colour under 7:1. Stellars-tech passes 7.20-10.12:1. Workflow skill updated: charts tool mandatory, both `--colors` and `--colors-dark` required, unresolved warnings = workflow violation. Telegraph-pass on the three autobuild orchestrator YAML prompts dispatched in the background.

89. **Task - Test suite cull, L-connector edge-aware API, 3D primitive wireframes, article_02 image set** (v1.0.16 PyPI, plugins v1.3.5): Aggressive pytest reduction, cardinal-direction L-routing, hidden-edge rendering for cube/cuboid/cylinder, twelve new article_02 SVG/PNG pairs<br>
    **Result**: Test suite went from 523 -> 246 (-53%) across nine consolidation batches: `test_model.py` 57->13 via `@pytest.mark.parametrize` tables and scenario-collapsing; `test_fsm.py` 31->7 via parametrized event sequences with `final_state` assertions; `test_svg_tools.py` 281->75 via per-tool parametrize tables (`TestCalcPrimitives` `_ANCHOR_CASES`, `TestManifoldConnector` shape-type fan-out, `TestCheckCSS` rule/severity rows); `test_orchestrator.py` 148->55 via full-file rewrite with `_base_state`/`_rich_ctx_entry` factories and consolidated lifecycle scenarios. `calc_connector.py` gained `_resolve_l_endpoints_from_rects()`, `_rect_edge_midpoint()`, `_direction_to_cardinal()` plus `src_rect`/`tgt_rect`/`start_dir`/`end_dir` parameters and `--src-rect` / `--tgt-rect` CLI flags; emits warnings when cardinal directions omitted. `calc_primitives.py` Z-axis arrowhead fixed with `back_angle = math.atan2(-z_dy, z_dx)` so arrows point outward; axes stop at `back_angle - ARROW_LEN` to avoid protrusion; `gen_cube`/`gen_cuboid`/`gen_cylinder`/`gen_plane` render dashed hidden-edge wireframes in wire mode. Twelve new `docs/medium/images_article_02/` SVG+PNG pairs generated via the full callouts/manifold/primitives pipeline with stellars-tech theme: five-defects, three-pillars, six-phase-workflow, tools-inventory, failfirst-flow, theme-approval, charts-showcase, callouts-showcase, primitives-gallery, connector-modes, honeycomb, constellation. `09-callouts-showcase.svg` uses leaderless target-offset trick to position labels above shapes; `11-connector-modes.svg` uses the new cardinal-direction API; `10-primitives-gallery.svg` regenerated after coordinate tightening; `08-charts-showcase.svg` has minimal stroke-only dark-mode overrides. Journal skill collapsed from three entry tiers (Short/Normal/Extended) to two (Standard 70-120w / Extended 250-350w) with match-user-summary-length rule. Autobuild `app.yaml`/`phases.yaml` telegraph-rewritten 136/136 lines by background agent, `test_orchestrator.py::test_record_template_has_conditional_commit` relaxed to case-insensitive "no code changes" substring. `svg-standards/SKILL.md` and `workflow/SKILL.md` updated with L-route edge-aware API canonical pattern and leader/leaderless mode picking rules. PyPI `stellars-claude-code-plugins` v1.0.16 published, plugins v1.3.5 across all six `plugin.json` files and every marketplace entry. 246 tests green at release.

90. **Task - Empty-space `container_id`, multi-elbow L-route auto-router (A*), callouts container passthrough** (v1.0.17 PyPI, plugins v1.3.6): `find_empty_regions` gained a `container_id` parameter that clips detection to the interior of any closed SVG shape; `calc_l` / `calc_l_chamfer` gained an `auto_route=True` mode that runs grid A* on the SVG obstacle bitmap and returns multi-elbow waypoints that avoid collisions; `propose_callouts` pipes `container_id` through to both its internal `find_empty_regions` call and the leader-walker free mask so callout placement can be clipped to a specific card interior<br>
    **Result**: Architectural change driven by the routing and callout limitation that the empty-space detector only ever saw the entire canvas as one flat space. Feature set shipped as one release after a four-phase plan: (1) `calc_l` multi-elbow `controls` support already existed from an earlier session via `_thread_l_controls`, verified working + soft-cap warning at >5 points; (2) new helper `_container_interior_surrogates(elem)` in `calc_empty_space.py` extracts closed-shape geometry from rect/circle/ellipse/polygon/polyline/path elements ignoring source fill/stroke attributes, with a deliberate `ValueError` when `container_id` resolves to a `<g>` because groups have no geometry (`<g>` is not a closed shape - its bbox is just the children's union, no boundary, no interior). `find_empty_regions` looks up the container via `svg_doc.get_element_by_id(container_id)`, skips the container's OWN surrogates during the obstacle walk while still recursing into children, rasterises the interior mask separately, AND-s it with the free bitmap BEFORE the distance-transform erosion so the container boundary becomes an implicit obstacle, and tags every returned region with `container_id: str|None` (backward-compatible additive key). (3) New `_auto_route_l_waypoints()` in `calc_connector.py` runs 4-connected Manhattan A* on a downsampled free mask: rasterise obstacles via `_element_to_surrogates`, erode by `margin_px` (default 5), downsample at `cell_size` (default 10 px), force-free a 3x3 halo around src/tgt cells to let the router enter and exit the endpoint rects' erosion shadows, Manhattan heuristic + incremental tiebreak counter for stable open-queue ordering, path reconstruction via `came_from` dict, collinear cell collapse to extract elbow corners only, drop of endpoint corners so waypoints become interior elbow list. The halo fix was the critical bug - without it, A* found the src cell "free by force" but all its neighbours were blocked by the 5 px erosion halo of the src rect, making the router report "unroutable" even on trivial scenes. `calc_l` / `calc_l_chamfer` gained `auto_route`, `svg`, `container_id`, `route_cell_size`, `route_margin` parameters; unroutable case falls back to 1-bend L with a warning in `result["warnings"]` so callers can inspect and retry. (4) `propose_callouts` accepts `container_id`, resolves the element once, passes the container_elem to `_build_obstacle_mask` (which now skips-self), AND-s the container interior mask into every standoff-specific free mask, and forwards `container_id` to the nested `find_empty_regions` call so the returned `empty_regions` polygons carry the tag. CLI flags wired end-to-end: `empty-space --container-id`, `callouts --container-id`, `connector --auto-route --svg --container-id --route-cell-size --route-margin`, with `--auto-route` requiring `--svg` via `parser.error`. Tests added: `test_empty_space_container_id` covers rect/circle/polygon/path containers, missing-id ValueError, group-rejection ValueError, backward-compat None tag; `test_auto_route_l` covers U-shape detour around a blocking obstacle (4 waypoints), inside-container routing staying within card bbox, and unroutable fallback; `test_auto_route_cli` exercises the full CLI path plus `--auto-route` without `--svg` parser error; `test_empty_space_cli` extended to cover the `--container-id` CLI flag. `svg-standards/SKILL.md` gained three new sections: "Multi-elbow L via `controls`", "Auto-route (A*) for L-routes" with the canonical command, and "Container-scoped routing and detection" with the picking rule "container ID must name a shape whose interior contains BOTH endpoints". `workflow/SKILL.md` tool inventory entries for `connector`, `empty-space`, and `callouts` rewritten to include the new flags. Released as v1.0.17 PyPI and plugins v1.3.6. 239 tests green at release.

91. **Task - Auto-skip full-canvas background plates, turn-penalty A*, two-card routing demo** (v1.0.18 PyPI, plugins v1.3.7): `find_empty_regions`, `propose_callouts._build_obstacle_mask`, and `_auto_route_l_waypoints` now auto-skip any element that covers >=80% of the canvas, eliminating a silent failure mode where a single `<rect class="bg-plate" width="W" height="H">` would mark every pixel as an obstacle and make the router report "unroutable" on otherwise trivial scenes. The A* cost function gained a turn penalty so the router prefers long straight runs over staircases<br>
    **Result**: Bug surfaced when building a two-card demo (`docs/examples/container-routing/two-cards-auto-route.svg`) with a full-canvas bg-plate, two rounded cards, and four obstacles inside `card-A`. Trace showed `free-inside-card px: 0` because the bg-plate `<rect>` with CSS-resolved `fill: #ffffff` was rasterised as the first obstacle and filled the entire grid. Added `_is_canvas_background(elem, canvas)` helper in `calc_empty_space.py` that checks if an element's filled bbox covers >=80% of the canvas area - same threshold `check_contrast._shape_is_doc_background()` already uses, so the two tools agree on what counts as the doc background. Wired into all three obstacle walks via a one-line `not _is_canvas_background(node, canvas)` condition. No opt-out flag - the behaviour is always on. Two legacy tests in `TestCalcConnectorModule` that relied on the old "rect covering 100% of canvas means zero free regions" semantics updated to use sub-80% obstacles (70x100 on 100x100, 320x220 on 400x300). New `test_empty_space_auto_skip_background` covers the full skip path (full-canvas rect -> whole canvas free), the 79% edge-case (NOT skipped), and a direct `_is_canvas_background` unit assertion. New `test_auto_route_skips_bg_and_routes_inside_card` is a regression for the demo failure - scene with bg-plate + card-A + 4 obstacles + src/tgt must route without warnings. Separately, the A* search was producing 9-13 waypoint staircases on the demo scene because Manhattan heuristic leaves horizontal and vertical steps equally attractive. Added a turn penalty: A* state expanded from `(cell,)` to `(cell, incoming_direction)` so moving in a different direction than the last step costs `TURN_PENALTY=4` extra cells. Same scene with cs=16 now produces 4 waypoints instead of 9. The heuristic stays admissible because Manhattan distance is a lower bound on cost regardless of penalty. Demo saved to `docs/examples/container-routing/two-cards-auto-route.{svg,png}` showing two connectors: C1 is the intra-card auto-route that threads through the `y=248` corridor between obstacle 1 and obstacles 2/3, runs down the right side of card-A, and enters `tgt` from below (`end_dir='N'`); C2 is a simple 1-bend card-edge L from `card-A` right edge to `dst` inside `card-B` (no auto-route needed). 241 tests green at release. `svg-standards/SKILL.md` container-routing section kept tool-usage-only per user direction - no internal heuristics leaked into the skill doc.

92. **Task - L-connector threader rewrite, straight-line collapse, stem preservation** (v1.0.19 PyPI, plugins v1.3.8): Rewrote `_thread_l_controls` as a single-pass orthogonal polyline builder with global chamfer, added straight-line collapse and stem reservation to L-routes<br>
    **Result**: Rewrote `_thread_l_controls` in `calc_connector.py` to build one continuous sharp polyline through all anchors, straighten collinear runs, then chamfer every 90-degree corner globally - fixing four artefacts from the old per-segment concatenation approach (inter-segment joins stayed sharp, `start_dir`/`end_dir` re-inferred per segment, chamfer overshoot on tiny deltas, 1-bend fallback bypassed `end_dir`). Added `first_reserve`/`last_reserve` to clamp outer bevels so standoff + head_len + stem_min always fits the final cardinal leg. Added `_rect_edge_slide_range` and `_rect_edge_point_at` helpers plus a 3-pass slide resolver in `_resolve_l_endpoints_from_rects` with `--straight-tolerance 20` (configurable): when both endpoints slide along same-axis edges within tolerance, the L collapses to a straight segment with bias toward the smaller geometry's midpoint. Added `--stem-min 20` (configurable) reserving visible cardinal stem behind arrowheads via A* `STEM_TURN_PENALTY=100` inside `ceil(reserve/cell_size)+1` Manhattan cells of endpoints, plus cell-centre waypoint snap so A* waypoints match threader corners exactly - eliminates the coordinate-system mismatch where A* measured distance to `tgt_cell_center` but the threader measured to `real_tgt`, silently losing up to `cell_size/2` px of stem. Demo `14-container-routing.svg` regenerated: C1 has full 5 px chamfer on all elbows with 26 px visible stem, C2 collapses to a straight horizontal via slide bias (dst-B stays put, card-A slides 8 px on its 380 px edge). Added `--straight-tolerance` and `--stem-min` CLI flags. Skills updated in `svg-standards/SKILL.md` and `workflow/SKILL.md`. 12 new regression tests, 266 green total. Commit `de82c16`, PyPI v1.0.19, plugins v1.3.8.

93. **Task - Journal tools CLI module, README updates** (v1.0.20 PyPI, plugins v1.3.9): Added deterministic `journal-tools` CLI (check/archive/sort) and updated marketplace + journal READMEs and journal skill<br>
    **Result**: New `stellars_claude_code_plugins/journal_tools.py` module with `parse_journal`, `check_journal`, `sort_entries`, `render_entries`, `archive_journal` functions. Parser extracts structured `JournalEntry` objects from JOURNAL.md via regex. Checker validates continuous numbering, ascending order, entry format, and word counts against two tiers (standard <=150 words warning, extended <=400 words error). Sorter re-numbers entries sequentially. Archiver moves entries beyond threshold to JOURNAL_ARCHIVE.md preserving numbering. CLI registered as `journal-tools` entry point in `pyproject.toml`. Journal skill updated to mandate `journal-tools check` after writing entries and prefer `journal-tools archive` over manual edits. Journal README updated with CLI tools section and word-count tier table. Marketplace README updated: svg-infographics row now covers 5 routing modes, A* auto-routing, callout solver, charts, and 6 checkers; journal row mentions deterministic CLI. Initial check on real journal found 11 entries over extended-max and 32 over standard-target. 266 tests green.

94. **Task - Article 02 SVGs 15-17**: Created three new SVG infographics for article_02_svg-infographics.md covering design-app analogy, algorithm stack, and self-documenting files<br>
    **Result**: Created `15-design-app-analogy.svg` (1000x400, two-column 5-row card-pair layout mapping Inkscape affordances to CLI equivalents with arrow connectors), `16-algorithm-stack.svg` (1000x380, four horizontal layer cards bottom-to-top showing rasterisation/pathfinding/interpolation/validation with library tags and progressive opacity), `17-self-documenting.svg` (1000x420, file icon with three dashed-line callout cards for XML comments/CSS classes/named elements, plus horizontal timeline arrow for reworkability). All three follow Stellars-Tech theme with full `@media (prefers-color-scheme: dark)` blocks, grid reference comments, layout topology comments, transparent backgrounds, Inter font throughout. Validation: overlap checker found one real issue (abstraction label y-position in SVG 2, fixed by shifting to y=368), all sibling overlaps are checker-padding artefacts (actual inner gaps 8-10px). Contrast checker: 0 FAIL, all warnings are AA-only (6.2-6.65:1 ratios on fg-3 text, consistent with all existing article SVGs). CSS checker warnings are a known checker limitation (cannot detect dark mode block inside `<style>` element, confirmed by running on existing `14-container-routing.svg` which shows identical warnings). PNGs rendered at 3000px width via cairosvg. Files at `docs/medium/images_article_02/{15,16,17}-*.{svg,png}`.

95. **Task - Embroidery glyphs showcase SVG**: Created `18-embroidery-glyphs.svg` - 32 decorative glyphs in 8x4 grid for the add-life embroidery command catalogue<br>
    **Result**: Created `docs/medium/images_article_02/18-embroidery-glyphs.svg` (1000x520, 626 lines, 40KB) with 32 stroke-based glyphs across four domain rows: electronics/circuit (chip, resistor, capacitor, transistor, led, antenna, pcb-trace, signal), AI/neural (neuron, network, layer, tensor, gradient, attention, prompt, embedding), science/math (atom, molecule, flask, graph, sigma, integral, infinity, dna), abstract/decorative (burst, spiral, constellation, wave, diamond-grid, crosshatch, rings, particle). Grid uses 110x106px cells with 12px gaps, symmetric 18px margins. Each cell has alternating teal/ochre background plates (fill-opacity 0.04) with 3px accent bars. Glyphs cycle through 14 colour classes (6 teal shades, 4 ochre shades, 2 hue-shifted green, 2 hue-shifted amber) with `feGaussianBlur` glow filter on alternating cells. Full dark mode via `@media (prefers-color-scheme: dark)`. Validation: contrast checker 34/34 text pass WCAG AA, overlap checker 133 overlaps all contained (glyphs inside plate groups), alignment checker 185 grid-snap warnings accepted (grid uses 12px gap not 5px). PNG rendered at 3000px width via cairosvg. Files at `docs/medium/images_article_02/18-embroidery-glyphs.{svg,png}`.

96. **Task - draw.io shape library module** (v1.0.20): Created `drawio_shapes.py` - draw.io stencil library indexing, searching, catalogue, and rendering as a second-tier shape source for svg-infographics<br>
    **Result**: Implemented `stellars_claude_code_plugins/svg_tools/drawio_shapes.py` (830 lines after ruff format) with five functional areas. `DrawioShape` dataclass holds name, category, library, dimensions, paste-ready `<g>` SVG snippet, and nine auto-derived cardinal anchor points. `ShapeIndex` dataclass provides `search` (substring, exact-first ordering), `list_categories`, `by_category`, `save` (JSON v1), and `load` class methods. `parse_drawio_library` handles Format B stencil XML (`<shapes><shape>`) and Format A mxlibrary JSON (`<mxlibrary>[...]</mxlibrary>`); both return empty lists gracefully on missing/malformed files. `_mxgraph_to_svg_path` converts stencil foreground/background elements (move/line/quad/curve/close/ellipse/rect) to SVG path `d` strings with coordinate scaling from the [0,100] stencil box. `build_index` walks directories recursively collecting all `.xml` files. `render_shape` wraps a shape in a `translate+scale` transform returning a `calc_primitives`-compatible dict. `render_catalogue` produces a full SVG grid with stellars-tech palette and dark-mode CSS. CLI exposes four sub-verbs: `index`, `search`, `catalogue`, `render`. Registered as `shapes` subcommand in `cli.py`. Added 38 tests across 5 test classes in `test_svg_tools.py` covering parsing, mxGraph conversion, index build/save/load/search, shape rendering, and catalogue output. All 298 tests pass; `drawio_shapes.py` passes ruff format and lint clean.

94. **Task - New primitives, draw.io shape catalogue, add-life command, article rework** (v1.0.23 PyPI, plugins v1.3.10): Added gear/pyramid/cloud/document primitives (18 built-in shapes total), draw.io stencil library indexer with on-demand download + cache, `/svg-infographics:add-life` creative enhancement command, article rewrite as "Stop Fixing Your AI's SVGs"<br>
    **Result**: Four new `gen_*` functions in `calc_primitives.py` (gear with configurable teeth, isometric pyramid with hidden-edge wire mode, cubic-bezier cloud, dog-ear document) + CLI registration + 5 tests. New `drawio_shapes.py` module (830 LOC): parses Format A (mxlibrary JSON) and Format B (stencil XML), `_mxgraph_to_svg_path` converter, `ShapeIndex` with fuzzy search/save/load, `render_shape` returning primitives-compatible contract, `render_catalogue` for visual SVG grids, `_resolve_source` downloads URLs to `~/.cache/svg-infographics/drawio-stencils/` on first use. CLI: `shapes index/search/catalogue/render`. New `add-life` command with 6 enhancement dimensions (colour/shapes/icons/embroidery/abstract/glow) x 4 levels (low/medium/high/absurd) + mandatory `<!-- add-life -->` documentation comment. Article reworked: "design application for agents" framing, use-case-driven sections, Nova Canvas hero image, 19 images including embroidery glyph gallery. Skills updated with 18-shape inventory + draw.io catalogue + add-life section. 307 tests green.

97. **Task - Drawio shapes download-on-demand rewrite**: Rewrote `drawio_shapes.py` to download stencil libraries on demand instead of bundling XML files with the pip package<br>
    **Result**: Rewrote `stellars_claude_code_plugins/svg_tools/drawio_shapes.py` from a directory-scanning model to a download-on-demand cache model. Key changes: removed `build_index()` (scanned directories), `ShapeIndex.save()`/`.load()` (explicit file I/O), and the `_INDEX_VERSION = 1` format. Replaced with `index_source(source)` accepting both URLs and local paths - URLs are downloaded via `urllib.request.urlretrieve` to `~/.cache/svg-infographics/drawio-stencils/`, local paths are parsed directly. `_load_index()`/`_save_index()` read/write the cache-resident `_index.json` (version 2) which now includes a `sources` dict tracking original URLs for each library, enabling `--refresh` to re-download all previously indexed libraries. `_resolve_source()` dispatches URL vs local path. `_no_index_hint()` prints a helpful message when the cache is empty, listing the default URL and other available libraries. Fixed `_mxgraph_to_svg_path` to use absolute coordinates instead of the previous normalised 0..100 scaling which produced incorrect paths for real draw.io stencils. Category detection now uses the `<shapes name="...">` attribute when present (real libraries use `mxgraph.basic`, `mxGraph.flowchart` etc), falling back to filename stem. CLI updated: `--source` and `--output`/`--index` flags replaced with cache-centric `--source` (URL or path) and `--refresh`, `--json` on render, `--output` optional on catalogue (prints to stdout by default). All 16 existing drawio tests updated for the new API (monkeypatched CACHE_DIR/INDEX_FILE for isolation) plus 3 new tests (empty cache, reindex deduplication, JSON structure v2). 297 tests green, ruff clean.

98. **Task - Mid-tier embroidery glyphs SVG**: Created `19-embroidery-glyphs-midtier.svg` - 32 intricate sci-fi/cyberpunk glyphs in 8x4 grid for mid-tier add-life embroidery<br>
    **Result**: Created `docs/medium/images_article_02/19-embroidery-glyphs-midtier.svg` (1000x560 viewBox, 66KB) with 32 multi-layered glyphs significantly more complex than the basic tier (18-embroidery-glyphs.svg). Four domain rows: brain/neural/AI (neural-core, synapse-burst, deep-stack, embedding-field, attention-eye, transformer, neural-mesh, thought-bubble), cyberpunk HUD (hud-bracket, scan-line, data-stream, target-lock, hex-shield, power-bar, circuit-corner, holo-frame), sci-fi abstract (warp-gate, quantum-bit, plasma-arc, gravity-well, energy-flow, dimension-fold, beacon, phase-shift), decorative flourishes (tech-vine, data-rain, fractal-branch, wave-interference, crystal-lattice, pulse-ring, nano-pattern, cyber-mandala). Each glyph uses 3-8 SVG elements with multiple stroke weights (0.5-1.8), 14 teal and 6 ochre stroke/fill classes, two glow filters (`glow-cyan` and `glow-warm` via feGaussianBlur), 6 gradient defs (3 linear + 2 radial + 1 horizontal), opacity layering (0.04-0.8), and 2-3 colours per glyph. Grid: 110x116px cells, 12px gaps, 18px symmetric margins. Full `@media (prefers-color-scheme: dark)` with all 33 classes overridden. Validation: overlap checker 134 all contained (glyph elements inside plate groups), 0 sibling overlaps. CSS checker: 16 warnings all checker limitation (cannot detect dark mode inside `<style>` element, same as basic tier). PNG rendered at 3000x1680 via cairosvg. Playwright visual verification confirmed all 32 glyphs visible and properly rendered.

95. **Task - Manifold crossing detection, skill header rewrite, embroidery galleries, article polish** (v1.0.24 PyPI, plugins v1.3.11): Manifold default tension raised 0.5->0.75, added pairwise strand crossing detection via `shapely.crosses()` and backward-curve detection via dot-product against spine flow, both emitting fix-instruction warnings. Skill header rewritten as design-application tool reference with two tables (6 design tools + 6 quality checkers) and "agent is designer, tools are application" framing. Principles condensed 14->10 with "tool first" as #1. Added generic text-after-visuals placement rule. Removed case-specific padlock note. 07-theme-approval.svg reworked with manifold spline fan-out (agent-delegated). Two embroidery galleries shipped: 18-basic (32 stroke glyphs) and 19-midtier (32 intricate sci-fi/cyberpunk with glow filters). Article cover regenerated via Nova Canvas. 307 tests green.

96. **Task - SVG designer agent, TOOLBOX tree, alignment tools, grouping, examples index** (v1.0.25 PyPI, plugins v1.3.12): Added svg-designer as delegable forked agent, TOOLBOX.md as tree-view tool palette inside svg-standards skill, geom align/distribute/stack for multi-rect layout, grouping elevated to core principle #4, and examples INDEX.md<br>
    **Result**: New `svg-designer` skill with `context: fork` for delegation via `Agent(subagent_type="svg-designer")`. TOOLBOX.md moved into `svg-standards/` as ASCII tree of all tools (18 primitives, 5 connector modes, 20+ geom ops, callouts, empty-space, charts, shapes, 6 validators, 6 commands) with quick-lookup table. Added `align()`, `distribute()`, `stack()` to `calc_geometry.py` accepting rect lists and returning adjusted positions; CLI subcommands `geom align/distribute/stack`. Grouping elevated to principle #4 in svg-standards: mandatory topology comment declaring group relationships, group pattern table, build-at-origin + translate workflow, nesting max 3 levels. Examples INDEX.md categorizes 66 SVGs by type. Embroidery galleries (basic + sci-fi midtier) added to examples/ as 65 and 66. 307 tests green.

99. **Task - Playwright render-png rewrite** (v1.0.26 PyPI, plugins v1.3.13): Replaced Chrome CLI subprocess in `render_png.py` with Playwright Python sync API to fix broken dark-mode rendering<br>
    **Result**: Chrome headless couldn't cleanly trigger `@media (prefers-color-scheme: dark)` - `--force-dark-mode` activates auto-darkening that corrupts colours, and the `_swap_dark_mode_css()` function called on line 86 was never defined (instant NameError on any dark-mode render). Researched alternatives: resvg and cairosvg both skip `@media` rules entirely (static renderers with no media query evaluation). Playwright solves both problems natively: `browser.new_context(color_scheme="dark")` triggers the CSS media query via CDP `Emulation.setEmulatedMedia`, and `page.screenshot(omit_background=True)` produces transparent PNGs. Rewrote `stellars_claude_code_plugins/svg_tools/render_png.py` from 210 lines of Chrome subprocess code to 145 lines of Playwright sync API. Removed `_find_chrome()`, Chrome flags, dead `_swap_dark_mode_css()` call. Added `playwright` to `pyproject.toml` dependencies. Playwright browsers already cached at `~/.cache/ms-playwright/` (chromium-1208, 280MB). Screenshot `clip={}` parameter handles viewport cropping directly, eliminating the Pillow post-crop step. Same CLI interface preserved: `render-png input.svg output.png --mode light|dark|both --width 3000 --bg "#0a1a24"`. Verified on `01-five-defects.svg`: light/dark/both modes render with correct colours, transparent backgrounds, no SVG modification. 307 tests green.

100. **Task - render-png skill and toolbox integration** (v1.0.27 PyPI, plugins v1.3.14): Updated medium-article skill, svg-designer skill, and TOOLBOX.md to reference `render-png` instead of cairosvg<br>
    **Result**: Replaced cairosvg PNG generation instructions across three files. `~/.claude/skills/medium-article/SKILL.md` now uses `render-png input.svg output.png --mode both --width 3000` instead of `cairosvg --output-width --background "#ffffff"`. `svg-infographics/skills/svg-designer/SKILL.md` rendering section rewritten with `render-png` usage and options. `svg-infographics/skills/svg-standards/TOOLBOX.md` gained `render-png` entry in tool tree (with `--mode`, `--width`, `--bg` sub-flags) and quick-lookup table row for "Export SVG to PNG". 307 tests green.

101. **Task - Add-life command rewrite, empty-space edges-only, background generator, article quickstart** (v1.0.28 PyPI, plugins v1.3.15): Rewrote add-life command with interactive proposal workflow (7 dimensions, user asked before applying), added `--edges-only` to empty-space tool, created procedural background geometry generator with 11 types, added article quickstart section<br>
    **Result**: `add-life.md` rewritten with mandatory proposal table (user confirms before edits), 7 dimensions (colour, shapes, icons, embroidery, abstract, bg texture, glow), soft-cap levels (no hard numbers - Claude decides), empty-space placement rules (must use `empty-space --edges-only --container-id` before placing any decoration), topology alignment requirement (icons across cards must be h-aligned via `geom align`). Added `--edges-only` flag to `calc_empty_space.py` `find_empty_regions()` - ignores filled backgrounds, only treats strokes/text/outlines as obstacles, enabling decoration placement WITHIN cards. New `gen_backgrounds.py` (11 types: circuit, neural, topo, grid, organic, celtic, scifi, constellation, flourish, geometric, crystalline) returning `BackgroundResult` with `BackgroundElement` primitives in same contract as `calc_primitives` - unstyled SVG geometry that Claude styles with CSS classes. Circuit uses tree branching with 45-deg diagonals, neural uses space colonization, topo uses opensimplex noise + matplotlib contour extraction with military-map style at high density, constellation uses scipy.spatial.Delaunay, crystalline uses scipy.spatial.Voronoi. Gallery example #67 generated (32 cells, 8x4). Article quickstart added with `/svg-infographics:create` command. Cover image updated with "Computed SVG Infographics for Agents" subtitle. Embroidery duplicates removed from article images (kept in plugin examples 65/66). New `/svg-infographics:export-png` command. svg-standards principle #2 "place via empty-space" added. 307 tests green.

102. **Task - Circuit and neural bg generator quality rewrite** (v1.0.29 PyPI, plugins v1.3.16): Full rewrite of circuit and neural background generators with grid-annealing, density gradients, right-skewed thickness, and density-driven branching/merging<br>
    **Result**: Circuit generator `_gen_circuit` in `gen_backgrounds.py` rewritten from flat bus loop to grid-annealing algorithm with 3x3 coarse grid and 2 rounds of 2x2 subdivision. Added 13 new parameters: `--origin-directions` (arbitrary angles, default `0,180`), `--bend-angle` (45-deg default for PCB routing), `--stroke-width`/`--stroke-std` with right-skewed distribution via `_right_skewed()` (power transform, skew=1.8 - many thin traces, fewer thick trunks), `--pad-radius`/`--pad-std` (grouped per bus, not dispersed), `--branch-prob`/`--merge-prob`, `--density-gradient` (6 presets: left-to-right, right-to-left, top-down, bottom-up, center-out, edges-in), `--density-points` (custom IDW interpolation), `--subdivision-rounds`. Density gradient amplified with `dw^2` for dramatic spatial variation. Collision avoidance uses clearance proportional to stroke width. Merge connections skip occupancy for short segments (<40px) since they intentionally bridge traces. Branch stubs use `_clear_branch` which skips first 30% of segment. Neural generator `_gen_neural` rewritten with density-driven attraction/repulsion: `attract_repel` ratio computed from local density - sparse areas boost repulsion (branches spread to fill space), dense areas boost attraction (branches converge and merge). Branches forced perpendicular (60-120 deg from parent) to create wide dendritic spread. Stroke width tapers root-to-tip (`tw * 0.88^depth`). Post-growth merge pass connects nearby branch tips. Performance fixed by capping repulsion scan to last 60 points (was O(n) over all_pts causing 50x slowdown). Article updated: "How it was developed" section added (empirical failure-mode methodology), bridge text between consecutive images, tools section prose restored, 14-container-routing.svg bg-plate set to transparent, 15-design-app-analogy.svg connectors fixed with 5px standoff. 307 tests green.

103. **Task - Manifold snapping rules, T-junction geometry, container-overflow validator, article visuals** (v1.0.31 PyPI, plugins v1.3.18): Rebuilt manifold connector semantics with geometric T-junction detection, added a DOM-based container-overflow validator, reworked article 02 visuals for chapters 17-19, and cleaned up PNG rasterisation policy<br>
    **Result**: `calc_manifold()` in `calc_connector.py` gained three layers of geometric intelligence. SNAPPING RULES project merge/fork points perpendicularly onto the spine segment, clamp to the spine bounds, and optionally grid-snap along the spine axis via `--snap-grid N`. Interior projections become T-JUNCTIONS: strand approaches spine perpendicular, end_dir inherits the perpendicular angle, chamfer drops to zero. Added `_project_onto_spine`, `_distributed_convergence_points`, `_snap_to_grid`, `_middle_alignment_hint`, `_endpoint_clearance_hints` helpers plus new `T_JUNCTION_EPS` threshold. Middle-connector detection generalised: for endpoint-convergence clusters, the strand whose perpendicular position is not at either extreme of the cluster drops its chamfer because it sits on a perpendicular trunk formed by its peers (18's 4 starts converging at spine_start = trunk of the T, middle two rows drop chamfer, extreme rows 1 and 4 keep it). `--strict` flag opts out of all geometry modification (spine projection, grid snap, T-junction chamfer suppression) while keeping the CONSIDER hints firing so Claude still sees suggestions. Inner-strand standoffs set to 0 so manifold strands join seamlessly with no 1px gaps at merge/spine/fork; outer-source standoff auto-scales to `end_standoff + head_len` producing symmetric visual gap at both ends of the manifold. Manifold sub-strand end_dir freed to `None` for L-chamfer endpoint convergence so the router honours start_dir without fighting a forced end_dir. CLI exposes `L-chamfer` as the preferred form (lowercase `l` reads as digit `1`). Telegram-style hint vocabulary - `WARNING:` for hard defects, `CONSIDER (snap rule):` for aesthetic suggestions, `HINT:` for auto-applied rules. Added flow-twist detection that warns when starts sit downstream of merges or ends upstream of forks (catches flipped spines). Stem-proximity hints fire when a bend lands within 3px of an arrow tip or start endpoint. New `svg-infographics validate` subcommand (`check_svg_valid.py`) checks XML well-formedness (catches `--` in comments, the #1 cause of broken article SVGs), viewBox presence, and empty `<path d>` attributes. New `svg-infographics overlaps` container-overflow check (`check_container_overflow` in `check_overlaps.py`) walks the SVG DOM, composes ancestor transforms (`_parse_transform` handles translate/rotate/scale/matrix), computes each text's final-rendered bbox in root coordinates, and compares against the enclosing `<g>`'s container rect - catches text escaping its card/zone boundary, including translated and rotated text. Fixed a `_find_chain` double-append bug that applied transforms twice. Added `tests/fixtures/container_overflow_test.svg` with 6 seeded fails (axis overflow, anchored-end left overflow, translated, rotated 90 deg, bottom overflow, nested compound transforms) plus 3 passing cases (inside, tiny sub-group below area threshold, exact-edge within 0.5px tolerance) and a matching `TestContainerOverflow` suite of 10 tests, all green. Documentation updates in `svg-infographics/skills/svg-standards/SKILL.md` adding Unicode-glyph rule (`→` over `->`, `↔` over `<->`, `•` over `*`, `…` over `...`, `×` over `x`) so SVG text uses proper glyphs. Documentation updates in `svg-infographics/skills/validation/SKILL.md` introducing the three-tier severity ladder (HARD FAIL / SOFT WARNING / HINT) and documenting five hard-fail classes (text-on-edge, edge-on-edge, text-outside-container, connector-through-content, XML malformation) with justification-by-XML-comment escape hatch. `medium-article` skill updated to STOP rasterising infographics to PNG - embed SVGs directly, only the cover PNG + titled-cover PNG remain. Article 02 assets: 17-self-documenting.svg rebuilt with clean single-spine manifold (middle end strand passes straight through, outer strands L-chamfer), 18-failure-driven-development.svg rebuilt with T-junction-middle chamfer suppression (middle 2 starts/ends = sharp L, extreme rows keep chamfer), new 19-scientific-method.svg depicts three-zone scientific pipeline (hypothesize prompts + generate N samples → measure 4 defect-class histograms with sigma badges → target high-variance quadrant scatter). Brightened article cover title to near-white cyan (#e6f8ff) with cyan glow halo plus warm orange (#ffbc6e) subtitle. Removed all auto-rendered PNG infographics from `docs/medium/images_article_02/` - keeping only `article-cover.png` and `article-cover-titled.png`. Test suite grew from 194 to 317 passing (added 10 container-overflow tests plus 113 tests from earlier work this session). Published as `stellars-claude-code-plugins v1.0.31` on PyPI, plugins bumped to v1.3.18.
